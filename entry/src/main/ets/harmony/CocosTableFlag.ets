/**
 * C2 Spike · Mode 2 table host switch.
 * false（默认）= 旧 ArkUI Table 表现；true = XComponent Cocos 宿主占位。
 * 无 K2 HarmonyOS NEXT 产物时保持 false，勿假装链接 libcocos。
 */
export const USE_COCOS_TABLE: boolean = false;
