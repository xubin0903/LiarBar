# 01 · 工程脚手架约定

| 项 | 内容 |
|----|------|
| 文档版本 | v0.2-tech-draft |
| 状态 | 约定草案 · **本阶段不提交可运行工程** |
| 对齐 | [工作流](../00-立项/02-制作人工作流与验收.md) · [数值表](../02-游戏设计/数值与牌堆配置.md) · [PRD](../01-产品/PRD.md) · [UI 命名表](../04-设计/02-组件与资源命名约定.md)（PR #3） |

> 读完应能回答：工程叫什么模块、配置键从哪来、资源怎么命名、分支怎么开。  
> **禁止**在本 PR 或本阶段往仓库塞 DevEco 工程、`ets/`、`hvigor`、可运行 HAP。

---

## 0. 阶段边界（先读）

| 现在做 | 现在不做 |
|--------|----------|
| 目录/模块命名、配置 JSON 草案、资源前缀 | 可编译工程、页面实现、规则引擎代码 |
| 与制作人工作流对齐的 `feat/client-*` | 改 GDD/PRD 规则、发明新胜负 |
| 工程引用 UI 已提交的 `lb_` id（屏/控件/美术槽） | 另起第二套前缀或临时文件名 |

分岗后由程序按本文 **另开** `feat/client-*` 实现分支开工；本文只当验收尺子。

---

## 1. 假设（已锁定，写进开工检查单）

| # | 假设 | 出处 | 工程推论 |
|---|------|------|----------|
| H1 | **手机竖屏优先**；折叠屏不承诺首版 | PRD §7 | 设计宽度按竖屏手机；不写平行视窗主路径 |
| H2 | MVP **只人机 + 本地** | PRD Q1、一页纸 §7 | 单模块即可；无房间服、无匹配 |
| H3 | MVP **无账号**；本地昵称 + 本地战绩 | PRD §9 | `Preferences` / 本地文件；不做登录页 |
| H4 | 规则数全部进配置表 | 数值表原则 | 业务代码禁止散落魔法数 |
| H5 | 当前文档阶段 **零工程代码** | 工作流 §1 | 本目录只有 Markdown |

---

## 2. 工具链与工程形态（建议）

| 项 | 约定 | 备注 |
|----|------|------|
| IDE | DevEco Studio（与课设机 HarmonyOS NEXT SDK 对齐，开工时写死小版本） | 本批不指定补丁号 |
| 语言 | ArkTS | 禁止用 JS 写规则引擎 |
| UI | ArkUI 声明式 | 对局桌走 `lb_scr_table`；路由字符串与 UI 命名表一致 |
| 包名建议 | `com.liarbar.app`（可改，改前同步答辩材料） | 英文工作名 LiarBar |
| 应用显示名 | **谎馆** | 口语「骗子酒馆」只出现在文案，不进包名 |
| HAP 形态 | MVP **单模块 `entry`** | 人机本地，不必拆 feature HAP |
| 最低适配 | 手机；横屏/平板/折叠 = 降级或裁切，不进 M1 退出标准 | 与 H1 一致 |

### 2.1 建议模块 / 目录（开工时再建，本文只锁名字）

```
LiarBar/                          ← DevEco 工程根（尚未创建）
├── AppScope/
│   └── app.json5
├── entry/
│   └── src/main/
│       ├── ets/
│       │   ├── entryability/     Ability 入口
│       │   ├── pages/            与屏 id 对齐：Lobby / Table / Recap / Settings
│       │   ├── features/         按玩法切片（非按控件切片）
│       │   │   ├── lobby/        F-01 开桌
│       │   │   ├── table/        对局桌、看牌、出牌包
│       │   │   ├── challenge/    F-05 三拍
│       │   │   ├── spectator/    F-06 旁观
│       │   │   ├── recap/        F-10 战报
│       │   │   └── tutorial/     F-12 教学
│       │   ├── engine/           规则状态机（纯逻辑，无 UI）
│       │   ├── ai/               荷官 + 三人格牌友（读配置表）
│       │   ├── config/           读 JSON；禁止在此写死规则数
│       │   ├── persist/          本地昵称 / 战绩 / 上次开桌配置
│       │   ├── harmony/          防窥 / 实况窗 / 智能填充 适配层
│       │   └── common/           枚举、日志事件、工具
│       └── resources/
│           ├── base/element/     字符串、色、浮点（手感阈值也可进 JSON）
│           ├── base/media/       占位图文件名 = UI `lb_art_*` 槽位
│           └── rawfile/config/   ★ 数值表 JSON（见 §3）
└── docs/                         本仓库文档（已存在；工程创建后勿复制进 HAP）
```

| 目录 | 职责 | 禁止 |
|------|------|------|
| `engine/` | 阶段转移、质疑判定、§10.1 | 调 UI、读窗口、调鸿蒙 Kit |
| `ai/` | 读人格概率表决策 | LLM 判真假/胜负 |
| `harmony/` | Kit 封装，失败可降级 | 把 Kit 调用散落到页面 |
| `config/` | 加载/校验 JSON | `if (lives === 3)` 写死 |
| `features/*` | 页面与交互 | 复制一份判定函数 |

**MVP 本地人机数据流**（约定，非实现）：

```
UI(features) ──意图──► engine ──快照──► UI
                 │
                 ├── ai（座位 role=AI 时）
                 ├── config（只读表）
                 └── persist（局后战绩；对局中不改规则）
```

荷官是 `engine` 的推进器 + 模板播报，不是独立服务器。

---

## 3. 配置表接入（JSON 化草案）

**原则（与数值文档一致）**：所有数进配置表；试玩只调表；锁定键改前过制作人。

### 3.1 文件建议

| 路径（开工后） | 内容 | 热更？ |
|----------------|------|--------|
| `resources/rawfile/config/match_defaults.json` | §3.2 对局基础 + 超时 | MVP 打进包 |
| `resources/rawfile/config/deck.json` | 牌堆构成、宣称集合 | 打进包 |
| `resources/rawfile/config/ai_personas.json` | 三人格概率 | 打进包 |
| `resources/rawfile/config/interaction_caps.json` | 敲桌/画✗ 等上限 | 打进包 |
| `resources/rawfile/config/demo_seed.json` | 演示种子开关与剧本钩子 | 打进包 |

开局时 **深拷贝进 `Match.config`**，本局内不再读文件（避免中途改表造成裁定漂移）。

### 3.2 键名（必须与数值文档一致）

下列键名 **逐字对齐** [数值与牌堆配置](../02-游戏设计/数值与牌堆配置.md)，禁止别名（如不要把 `lives_default` 写成 `hp`）。

```json
{
  "schema_version": "0.2",
  "min_players": 3,
  "max_players": 6,
  "default_players": 4,
  "lives_default": 3,
  "hand_size_default": 5,
  "max_play_cards": 3,
  "min_play_cards": 1,
  "turn_seconds": 15,
  "challenge_only_seconds": 8,
  "challenge_bet_seconds": 2,
  "challenge_ritual_max_seconds": 4,
  "low_time_threshold": 5,
  "timeout_auto_play": true,
  "max_slam_per_game": 5,
  "max_hesitate_per_game": 8,
  "hesitate_dwell_ms": 800,
  "soft_max_velocity": 1200,
  "slam_min_velocity": 2800,
  "demo_seed_enabled": true,
  "demo_seed_value": 20260906,
  "avoid_same_claim_streak": 1,
  "face_bet_enabled": true,
  "face_bet_correct": 2,
  "face_bet_wrong": 0,
  "auto_play_prefer_legal": true,
  "auto_play_else": "random_one",
  "challenge_only_on_timeout": "skip",
  "consecutive_timeout_warn": 2,
  "ai_think_ms_min": 400,
  "ai_think_ms_max": 1200
}
```

| 键 | 锁定？ | 程序禁区 |
|----|--------|----------|
| `lives_default` `hand_size_default` `min_play_cards` `max_play_cards` | **锁定** | 不得为「手感」私自改默认 |
| `challenge_only_seconds` | **锁定 8** | 不得改成回合 15s 混用 |
| `face_bet_enabled` / `face_bet_wrong` | **锁定** 开 / 0 | 押错不得扣分 |
| `demo_seed_enabled` / `demo_seed_value` | 默认 true / `20260906` | 演示包保持开启 |
| `turn_seconds`、Joker 张数、AI 概率 | 可试玩调表 | 只改 JSON |

### 3.3 牌堆 JSON 草案

```json
{
  "ranks": ["A", "K", "Q"],
  "wild": "JOKER",
  "claimable": ["A", "K", "Q"],
  "decks": {
    "3": { "A": 6, "K": 6, "Q": 6, "JOKER": 3 },
    "4": { "A": 8, "K": 8, "Q": 8, "JOKER": 4 },
    "5": { "A": 10, "K": 10, "Q": 10, "JOKER": 5 },
    "6": { "A": 12, "K": 12, "Q": 12, "JOKER": 6 }
  }
}
```

- `claimable` **不含** `JOKER`（G-Q7）。  
- 5～6 人桌为 P1；MVP 主路径读 `"4"`。  
- 重建发牌（GDD §10.1）：对**存活**座位按 `hand_size_default` 从 **完整** `decks[n]` 洗牌重发，不是从弃牌堆抽。

### 3.4 判定公式（只实现数值文档，不改语义）

```
is_legal(card, claim)      = card.rank == claim OR card.rank == JOKER
hand_is_clean(cards, claim)= 每张 is_legal
质疑成功                   ⇔ NOT hand_is_clean(上家本手, claim)
```

实现必须放 `engine/`，单测对着这三行；禁止 UI 层重写。

### 3.5 魔法数禁令（验收可勾）

- [ ] `entry/src` 内无 `15`/`8`/`3`/`5` 作为规则常量（枚举下标、数组长度除外）  
- [ ] 超时、命数、手牌、出牌张数、仪式秒数均来自 `Match.config`  
- [ ] SOFT/SLAM/HESITATE 阈值读 `soft_max_velocity` / `slam_min_velocity` / `hesitate_dwell_ms`  
- [ ] 新增键先改数值文档再改 JSON，禁止只改代码  

---

## 4. 资源 / 组件命名（工程只引用，不另起一套）

**权威表**：[04-设计/02-组件与资源命名约定](../04-设计/02-组件与资源命名约定.md)（PR [#3](https://github.com/xubin0903/LiarBar/pull/3)，分支 `feat/ui-wireflow`）。

- `develop` **尚未合入**该 PR 时：用 `gh` / 该分支文件，**不要**在本目录复制一份改名表。  
- 工程 `id`、路由、`media/` 文件名、自动化点选 **逐字引用** 该表。  
- **禁止**第二套前缀：不要 `lb_card_`、`lb_ui_`、`lb_seat_`、`liar_`、`xh_`、驼峰文件名、中文文件名。  
- 缺图：仍用该表文件名，放带 id 的灰底占位（UI 命名表 §0）。

### 4.1 分段（与 UI 表 §1 一致）

```
lb_<类别>_<对象>[_<变体>][_<状态>]
```

| 类别 | 前缀 | 工程用途 | 例（UI 已定，勿改） |
|------|------|----------|---------------------|
| 屏 | `lb_scr_*` | `NavDestination` / 路由 / 埋点 | `lb_scr_home` `lb_scr_lobby` `lb_scr_table` `lb_scr_peek` `lb_scr_play` `lb_scr_challenge` `lb_scr_recap` `lb_scr_settings` `lb_scr_tutorial` |
| 控件 | `lb_cmp_*` | 组件 `.id` | `lb_cmp_hand` `lb_cmp_card_{0-4}` `lb_cmp_peek_mask` `lb_cmp_topbar` `lb_cmp_nick_field` |
| 按钮 | `lb_btn_*` | 可点控件 `.id` | `lb_btn_quickstart` `lb_btn_challenge` `lb_btn_play_confirm` `lb_btn_challenge_skip` |
| 美术槽 | `lb_art_*` | `resources/base/media` 导出名 | `lb_art_card_back` `lb_art_card_a` `lb_art_dealer` `lb_art_challenge_bg` |
| 图标 | `lb_ico_*` | 宣称符号等 | `lb_ico_claim_a` |
| 文案 | `lb_str_*` | 字符串表 | `lb_str_challenge` `lb_str_challenge_disabled_first` `lb_str_hand_empty` |
| 音效 / 震动 | `lb_sfx_*` / `lb_hpt_*` | 调用名先写死 | `lb_sfx_peek` `lb_hpt_press` |
| 空位 | `lb_slot_*` | P1 不挂图 | `lb_slot_recap_vs` |
| 防窥调用点 | `lb_privacy_on` | 非可见控件；揭牌瞬间 | 见 `03` F-03 |

逻辑枚举（`SOFT` / `KNOCK` / `SMILE` / 阶段 ID）保持 GDD/互动方案大写；**资源文件仍小写**：`lb_art_play` 不用，出牌方式用 `lb_btn_style_soft` + `lb_sfx_soft`。

### 4.2 屏 id ↔ 阶段（工程路由）

与 UI 表 §2、GDD §4.1 对齐。阶段枚举仍是 `02` 的十态；屏 id 只是壳。

| 屏 id | 阶段 / 叠层 | 工程落点 |
|-------|-------------|----------|
| `lb_scr_home` | — | 快速开桌 `lb_btn_quickstart` |
| `lb_scr_lobby` | `LOBBY` | 人数/静默/`lb_cmp_nick_field` |
| `lb_scr_table` | `DEAL` `CLAIM` `TURN` `PENALTY` | 主桌 |
| `lb_scr_peek` | 叠在 table | 长按看牌 + `lb_privacy_on` |
| `lb_scr_play` | `PLAY_REVEAL_SELF` | `lb_sheet_play` 半屏 |
| `lb_scr_challenge` | `CHALLENGE_RITUAL` / `JUDGE` | `lb_cover_challenge` |
| `lb_scr_recap` | `RECAP` | 胜负只换 `_win`/`_lose` 顶区，屏 id 不变 |
| `lb_scr_settings` | — | `lb_tog_silent` |
| `lb_scr_tutorial_*` | 教学六步，不另开路由 | 见 UI 表；**禁止**第一手教质疑 |

### 4.3 状态后缀（控件与美术共用）

通用交互、命数、出牌方式以后缀换图，主名不变（UI 表 §3）。

| 场景 | 必须用的后缀 | 工程绑定 |
|------|----------------|----------|
| 默认可点 / 按下 / 禁用 | `_idle` `_press` `_disabled` | 所有 `lb_btn_*`；**第一手**质疑 = `lb_btn_challenge` + `_disabled`（文案 `lb_str_challenge_disabled_first`） |
| 手牌 | `_idle` `_press` `_selected` `_empty` | `lb_cmp_card_{0-4}` + `lb_art_card_*` |
| 仅质疑窗 / 超时 | `_timeout` `_empty` | 手牌 0：`lb_str_hand_empty`；窗结束 `_timeout` |
| **质疑三拍** | **`_challenge_windup`**（拍1 指控）· **`_challenge_reveal`**（拍2 对峙 + 拍3 翻）· **`_challenge_result`**（拍3 定格） | `lb_art_challenge_bg_*`、姿态/反应图；跳过用 `_challenge_skip`（可无独立图） |
| 裁定 | `_challenge_win` / `_challenge_lose` | 只表示本次质疑输赢，**不改判定公式** |
| 出牌方式 | `_soft` `_slam` `_hesitate` | 对齐 `SOFT`/`SLAM`/`HESITATE`；P1 `_pin`/`_blind` 预留不出芯片 |

对峙 **复用** `_challenge_reveal`，禁止工程自造 `_standoff`。

### 4.4 组件态 → UI id（对照，禁止再发明资源键）

| 逻辑态（`02` / 引擎） | 引用的 UI id | 说明 |
|----------------------|--------------|------|
| 手牌背面 | `lb_art_card_back` + `_idle` | 默认不可见牌面 |
| 长按揭牌 `PEEK` | `lb_scr_peek` · `lb_cmp_peek_mask` · `lb_art_card_{a\|k\|q\|joker}` · **`lb_privacy_on`** | 同时开 F-03 |
| 手牌多选 | 同正面 + `_selected` | `lb_txt_play_count` |
| 超选 / 非法 | `_disabled` | 张数超 `max_play_cards` |
| `canChallenge==true` | `lb_btn_challenge` `_idle` | |
| 第一手 / 无上家本手 | `lb_btn_challenge` `_disabled` | 勿另起 `*_off` / `*_first_play` 文件名 |
| `CHALLENGE_ONLY` | 仍是 `lb_btn_challenge`；手牌条 `_empty` | 文案用 `lb_str_hand_empty` |
| 出牌方式 | `lb_btn_style_soft` / `_slam` / `_hesitate` | 音效 `lb_sfx_soft` 等 |
| 实况窗 | 读 `lb_txt_timer` `lb_txt_claim` 同源数据 | 见 `03`；**禁止**牌面进窗 |
| 表情 | `lb_art_mask_smile` 等（UI 表 §5.2） | 与轮盘枚举同一空间 |
| 智能填充 | `lb_cmp_nick_field` | F-11 |
| 再来 / 回首页 | `lb_btn_again` `lb_btn_home` | `RECAP` |

完整控件清单以 UI 表 §4～§7 为准，本文不复抄以免两份漂移。开工检查：`entry` 里出现的每个 `lb_` 都能在 UI 表搜到。

皮肤：默认「夜半酒馆」只换 `lb_art_*` 像素；**id 不改**。校园皮同样。

---


## 5. 分支 / 目录约定（与制作人工作流一致）

摘自 [02-制作人工作流与验收](../00-立项/02-制作人工作流与验收.md)，客户端补充如下：

| 前缀 | 用途 | 本岗示例 |
|------|------|----------|
| `feat/design-*` | 策划 / 玩法文档 | 不由客户端开 |
| `feat/art-*` | 美术 | 不由客户端开 |
| `feat/ui-*` | UI 线框与命名 | id 权威在 `feat/ui-wireflow`（PR #3） |
| **`feat/client-*`** | 鸿蒙约定与日后实现 | **本批：`feat/client-tech-spec`**（仅 docs） |
| `feat/test-*` | 用例/试玩 | 状态机单测日后用 |

| 约定 | 说明 |
|------|------|
| 文档 PR 标题 | `docs:` / `docs(client):` |
| 合入目标 | 日常 PR 只开 **`develop`**；**禁止**开向 `main`（见 CONTRIBUTING） |
| 本批范围 | 只改 `docs/05-技术/` + 文档中心导航；**不改** GDD/PRD/数值规则正文 |
| 质量门 | **待 @LiarBar策划 群里会签**「规则可实现、特性服务玩法」后再请负责人终审 |
| 实现开工后 | 一特性一分支，例如 `feat/client-turn-ui`、`feat/client-privacy-peek` |
| 忽略规则 | 工程文件的忽略写在本地 exclude；密钥不进库（日后若有） |

```
读一页纸 → GDD → 数值 → PRD → 本目录
  → feat/client-* 产出
  → PR（写清对齐哪条决议）
  → 制作人审核 → 合格再给下游实现
```

---

## 6. 本地持久化（无账号）

| 键空间 | 存什么 | 不存什么 |
|--------|--------|----------|
| `lb.profile` | 昵称（可被智能填充，F-11） | 账号 token |
| `lb.last_table` | 上次人数/命数/静默局 | 他人手牌 |
| `lb.stats` | 胜场、撒谎次数等战报聚合 | 联网排位 |
| `lb.match_log` | 最近 N 局事件日志（供 F-10） | 原始牌面对外导出（演示调试可另开开关） |

对局中手牌只活在 `engine` 内存；局后日志按 GDD §7.2：PLAY **不写牌面**。

---

## 7. 开工检查单（给未来的自己）

- [ ] 从**最新** `develop` 拉 `feat/client-*`，rebase 后再开工  
- [ ] 建 `entry` 单模块；竖屏；无人机以外的座位用 AI 人格  
- [ ] 拷数值键进 JSON，跑一遍「无魔法数」检索  
- [ ] `engine` 单测覆盖：第一手不可质疑、只验本手、受罚者下家、§10.1 三分支  
- [ ] 所有控件/资源 id 能在 UI 命名表搜到；**零** `lb_card_` / `lb_ui_`  
- [ ] `harmony/` 三特性可降级：无 Kit 权限时局仍可打完  
- [ ] **仍然禁止**：联网房、反作弊、LLM 判胜负、商业化  

---

## 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-06 | 脚手架约定草案 | 新建；零源码 | 客户端文档 | 待审 |
| 2026-09-06 | 资源命名改引用 UI 表，不另起一套 | 重写 §4 | 客户端文档 | 待会签 |

---

*相关：[本目录 README](./README.md) · [02-回合状态机草图](./02-回合状态机草图.md) · [03-鸿蒙特性选型](./03-鸿蒙特性选型.md) · [UI 命名表](../04-设计/02-组件与资源命名约定.md)*
