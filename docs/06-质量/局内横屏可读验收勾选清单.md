# 局内横屏可读验收勾选清单

> ## 合入 ≠ 终验 · 未 Rebuild 勿勾
>
> **本表抄自 [12](../04-设计/12-局内横屏打回加严-验收.md) R-LAND-1～5**（#52 / `f9f8c0c`）。失败短句对齐 **12 v0.1.7（本 PR）**：**Rebuild 三闸**（手牌贴底只留缝 / 荷官禁顶飞 / 删首出 tip）。仍认 12 v0.1.6 的 R-LAND-3 **手牌-环隙锁 24%**（`HAND_RING_GAP` / `HAND_RING_GAP_PCT` = **24%**；**禁 padB** 顶高；原环隙数字让位）。仍认 12 v0.1.5 的 R-LAND-1 闪烁短句。仍认 12 v0.1.4 的水平中线锚（`selfDock` 不得挤中心）。根因 ②③ 认 R-LAND-3 + 11 **self 不压牌**，不另起闸。仍认 #58 / `81bf7c3`（12 v0.1.3）的裁半、环位、池旁张数。三座 idle 对齐美术 #54 / `a74eebdc`。**布局闸与听感分闸**；不开 13b。  
> **合入本文档 ≠ 客户端过关。** 等客户端合入 **且** Rebuild 到自称已落地打回加严的包，**现场过再勾**。未落地、热重载冒充、只合文档，**一律不得勾「本批过关」**。  
> **L1 / C1 / D1 终验仍暂挂**：须 **R-LAND-1～5 全过**，**且** 加严后的 O1 / LY1 / X1–X4 过，**且** 票 B 锚点现场仍落在同一中心，才重开。  
> **质疑继续冻。** 大厅 **G1–G4 / R1–R4 不重开**。本批 **不验收** 完整出牌 / 质疑流程。  
> **Cover 居中仍有效：** 继续竖底 `art_table_bg` + `ImageFit.Cover`（毡面安全带，11 §4.3）。**不新出横底。**

| 项 | 内容 |
|----|------|
| 版本 | **v0.2.5 · 抄 12 v0.1.7 失败短句**（**R-LAND-1～5** 本批硬门槛；R-LAND-3 环隙锁 **24%** + 贴底只留缝；R-LAND-2 荷官禁顶飞 + 删首出 tip；O1 / LY1 / X1–X4 **映射加严**；**未落地勿勾过关**） |
| 读者 | **用户本机终验** / 评委 / 测试岗 |
| 范围 | **只验** #50 / #57 横屏桌打回加严五门 + 仍有效的可读闸 + 本版 Rebuild 三闸。不改规则、不扩质疑、不重开大厅、**不开 13b** |
| 对齐 | [12 §3](../04-设计/12-局内横屏打回加严-验收.md) **R-LAND-1～5**（#52 / `f9f8c0c`）· 失败短句 **12 v0.1.7（本 PR）** · [11](../04-设计/11-局内横屏与自适应布局.md) 对照词（#53 / `4897657`；GAP=24% / 禁 padB 由 UI 岗回写 11，**本表不改 11**）· 三 idle #54 / `a74eebdc` · 旧表 #49 O1 / LY1 / X1–X4 **加严映射** · 票 B #48 / `f7c647b` **已合** |
| 不做 | 质疑四拍 VFX；大厅返工 / 强制横；另起加载闸；荷官新全身；改手牌 UX / C1；勾 L1/C1/D1 终验正文；新开 13b / 把听感并进 R-LAND-3 |

> **本批布局加严只认 R-LAND-1～5。** 只过旧 10 的 O1/X1 **不得**勾本批。失败短句用 §4 原句，不要写「挤了一点 / 还行」。

---

## 0. 对照词与旧号映射

| 对照词（12 / 11 同用） | 本批闸 | 旧号怎么用 |
|------------------------|--------|------------|
| **进桌立刻横** + **复用 L1 加载** | **R-LAND-1** | **O1 加严**：锁横提前到点 `lb_btn_quickstart`；叠层已横；**进桌后横屏稳定不闪**。废止「先进桌再拧 / 加载跟 lobby 竖」 |
| **荷官+牌堆** | **R-LAND-2** | **X2 加严**：已有 bust 必须可见，贴 `dealPileAnchor` / `lb_cmp_pool` 旁或上；**禁**被环-手空隙顶飞；池旁张数（`poolText`）贴池，不得落到 `lb_cmp_hand` 下；稳态桌心不得再出「第一手还没人出过牌」 |
| **手牌沉底留空** | **R-LAND-3** | **X1 形态加严**：**手牌贴底安全区**（底缘贴底安全区上沿，**只留缝**），**整张在区内**，与中央牌堆/环心留空约 **24%** 竖向间隙（`HAND_RING_GAP` / 手牌-环隙锁 **24%**）；**禁 padB 顶高**；手牌不得压牌堆/桌心提示；禁悬空偏高 / 底缘离底过大 |
| **手牌水平中线锚** | 加严挂 **R-LAND-3**（不另起闸） | `lb_cmp_hand` 中线 = 屏幕/桌面水平中线；张数变只左右伸缩、中心不漂；**不得被 `selfDock` 挤走** |
| **稳态无大双 CTA** | **R-LAND-4** | **X1 CTA 加严**：稳态不常驻大双底栏；弱入口或先藏可过 |
| 三座能分人 | **R-LAND-5** | 旧表无独立号；座次锁死下表；**环位写死**（左 p3 karen · 上 p2 shark · 右 p1 timid · 下手 self） |
| 约束 / Cover | **LY1**（仍有效） | 空隙不写 px；环隙锁 **24%** 窗口高；竖底 `art_table_bg` + Cover 居中仍有效 |
| 出牌包面积 | **X3**（仍有效） | **本批不强制打开**；若打开仍认 < 50% |
| 回开桌弱 | **X4**（仍有效） | 更禁通栏「回开桌」填双底栏空档 |

**座次锁（#52 / #54 · 一字不改）：**

| 座 | 控件 | 显示名 | 人格 | 媒体 id | 规格 |
|----|------|--------|------|---------|------|
| p1 | `lb_cmp_seat_p1` | 怂货 | timid / `AI_TIMID` | **`art_ai_timid_idle`** | **256 方透明**，挂 `lb_cmp_seat_*`；圆裁在 UI |
| p2 | `lb_cmp_seat_p2` | 老千 | shark / `AI_SHARK` | **`art_ai_shark_idle`** | 同上 |
| p3 | `lb_cmp_seat_p3` | 杠精 | karen / `AI_KAREN` | **`art_ai_karen_idle`** | 同上 |

`lb_cmp_seat_self` 仍系统头，**不**挂 `art_ai_*`。`*_hold` / `*_break` **本批不验**（质疑仍冻）。

**环位写死（#58 / 12 v0.1.3 · 对照 11 §2.2 · 不另起闸）：** 左 `p3` karen 杠精 · 上 `p2` shark 老千 · 右 `p1` timid 怂货 · **下手 self**。老千不见 / self 跑到中上 / 占位黑块减号头像 = 不过。

---

## 1. 过关闸（未齐不得勾「本批过关」）

| # | 闸门 | 必须同时满足 | 过 |
|---|------|--------------|----|
| **R-LAND-1** | 进桌立刻横 + 复用 L1 加载 | 点 quickstart **立刻**锁横；`lb_ovl_match_load` 级（动效 + BGM/氛围 + 轮换小提示）；**进桌后横屏稳定不闪**；大厅可继续竖 | [ ] |
| **R-LAND-2** | 荷官+牌堆 | 已有 bust 在牌堆旁/上可见；保留 `dealPileAnchor` / `lb_cmp_pool`；**禁**被环-手空隙顶飞；池旁张数贴池，不得下手牌；稳态无 tip「第一手还没人出过牌」；不新全身 | [ ] |
| **R-LAND-3** | 手牌沉底留空 + 水平中线锚 | 己方手牌再沉**贴底安全区**（底缘**只留缝**）；**整张在区内**；与中央牌堆/环心留空约 **24%** 竖向间隙；**禁 padB 顶高**；手牌不得压牌堆/桌心提示；中线 = 屏幕中线，张数变只左右伸缩、中心不漂；**`selfDock` 不得挤中心**；未沉底 / 间隙不足 24% / 贴桌心或整排上漂 / 中线右偏 / 裁半 / 半张出屏 / 底缘离底过大 / 悬空偏高不过 | [ ] |
| **R-LAND-4** | 稳态无大双 CTA | 稳态桌不常驻大块「质疑」「出牌」双底栏；竖向空间给手牌 | [ ] |
| **R-LAND-5** | 三座能分人 | p1 timid / p2 shark / p3 karen；**环位**左 p3 · 上 p2 · 右 p1 · 下手 self；禁方块减号 / 占位黑块减号 | [ ] |
| **本批加严过关** | 五门 | **R-LAND-1 ∧ 2 ∧ 3 ∧ 4 ∧ 5**。**未落地 / 未 Rebuild 不得勾本行** | [ ] **未落地勿勾** |
| O1 / LY1 / X1–X4 | 10 可读（加严口径） | 映射见 §0；**不得**用旧大双底栏交差 X1；L-small 隐 bust = X2 不过 | [ ] |
| **横屏可读过关** | 加严 ∧ 可读 ∧ 锚点 | 五门 + 加严后的六条 + 票 B 现场仍过。之后才重开 L1 / C1 / D1 | [ ] **暂挂终验** |

- 缺任一门 R-LAND = **本批横屏打回加严不过**。不得用「#50 / #57 能玩」交差。
- **合入客户端 ≠ 终验。** 现场 Rebuild 过再勾。
- **L1 / C1 / D1 仍暂挂。** 未走到上表「横屏可读过关」之前，开桌加载-牌面-发牌清单「本批过关」**不得新勾**。
- 大厅 **G1–G4 / R1–R4 不重开**。质疑继续冻。

---

## 2. 前置（不齐不勾过）

- [ ] 已 **Rebuild** 到**自称已落地横屏打回加严**的客户端（进桌立刻横 + 桌面分区 + 三 idle）。**有包再验**。不要热重载冒充；不要用只合 12/11/本表、桌面仍 #50 打回态的包勾过
- [ ] 大厅 **可以保持竖屏**。冷启动轴已结束，`lb_btn_quickstart` **已解锁可点**。**不要**先拧设备再点
- [ ] 锁名能对上：`lb_scr_lobby` / `lb_btn_quickstart` / `lb_btn_start` / **`lb_ovl_match_load`** / `lb_scr_table` / `lb_cmp_dealer` / `lb_cmp_pool` / `dealPileAnchor` / `lb_cmp_hand` / `lb_cmp_card_{0-4}` / `lb_cmp_seat_self` / `_p1` `_p2` `_p3` / `lb_btn_challenge` / `lb_btn_play` / `lb_txt_claim`
- [ ] 媒体能对上：`art_table_bg`（竖底 Cover）· `art_dealer_bust_idle` / `_announce` · **`art_ai_timid_idle` / `art_ai_shark_idle` / `art_ai_karen_idle`**
- [ ] **质疑四拍 / TC-19 / 旁观 / 改规则 继续挂起**；本批不验收完整出牌/质疑流程
- [ ] **L1 / C1 / D1 终验仍暂挂**；五门未齐之前，开桌加载清单「本批过关」**不得新勾**（L1 数字仍认 08，本表不重验毫秒）

---

## 3. 加严勾选（须 R-LAND-1～5 全过）

抄 [12 §3](../04-设计/12-局内横屏打回加严-验收.md)。**听 / 看 / 点**；失败短句必须是 §4 原句。未落地构建 **不得勾「过」**。

墙钟：点 `lb_btn_quickstart` 起 → 稳态桌（`CLAIM` / `TURN`）。发牌飞牌期仍走 D1，本表不替 D1。  
任一条失败 = **本批横屏打回加严不过**。

| ID | 对照词 | 点 | 看 | 听 | 失败短句 | 过 |
|----|--------|----|----|----|----------|----|
| **R-LAND-1** | **进桌立刻横** + **复用 L1 加载** | 大厅保持竖。冷启动轴已结束，点 `lb_btn_quickstart`（`lb_btn_start` 同一套）。**不要**先拧再点 | 主钮抬起后叠层**第一帧已横**，中间不得插入可操作竖桌。出现 `lb_ovl_match_load`：**桌布/牌堆在动**（禁单转圈）+ **底沿轮换小提示**（2～4 条，如「牌来了」「入座」）。切到 `lb_scr_table` 已是横桌，评委不用再转。回大厅可回竖。禁止整应用一刀切横。**进桌后横屏稳定不闪**；朝向只在进桌/离桌设置，禁止稳态轮询反复 `setPreferredOrientation`；禁止 200ms 级无差别 State 全树刷导致可见闪 | 非静默：叠层同起仍听 L1 的 `sfx_match_open`（布面/牌摞）；**夜馆垫/氛围不断成死寂**。静默：无声，画面仍播。**不在本条重验** L1 毫秒窗 | 先进桌再拧横；或大厅被逼必须横；或进桌后桌面闪烁 / 朝向或布局反复跳动 | [ ] |
| **R-LAND-2** | **荷官+牌堆** | **不点**。稳态桌盯桌心 | `lb_cmp_dealer` 挂已有 **`art_dealer_bust_idle` / `_announce` 胸像裁切**，在 `dealPileAnchor` / `lb_cmp_pool` **旁或上**（桌心牌堆旁）。能指两块。可缩小，**不可卸**。不是全身、不是空槽。L-small「隐 bust / 只留字幕」= 不过。**禁**被环-手空隙（`HAND_RING_GAP` 24%）顶飞离桌心。池旁张数（工程现挂 `poolText`）**贴池 / 锚点旁或上**，不得落到 `lb_cmp_hand` 下。**稳态桌心不得再出现** tip 文案「第一手还没人出过牌」 | 本条不听 | 荷官没了 / 只剩空槽；或池旁张数落到手牌下；或荷官被环-手空隙顶飞 / 不在桌心牌堆旁；或 tip 仍在 / 第一手还没人出过牌仍显示 | [ ] |
| **R-LAND-3** | **手牌沉底留空** + **手牌水平中线锚** | **不点**。眼看 `lb_cmp_hand` 底缘与中央牌堆 / 环心；张数变时再看整排相对屏幕中线；对照 `selfDock` 有没有把牌排挤右 | **两闸挂本条。** ① **再沉底**：己方手牌沉底可见，**贴底安全区**（底缘贴底安全区上沿，**只留缝**，不进手势条）；与中央牌堆 / 环心留空约 **24%** 竖向间隙（`HAND_RING_GAP` / 手牌-环隙锁 **24%**）；**禁 padB 顶高**；手牌不得压牌堆 / 桌心提示。② **水平中线锚**：**水平中心锚固定；张数变化只左右伸缩，中心不漂。** 中线 = 屏幕/桌面水平中线；**不得被 `selfDock` 挤走**。**稳态手牌须整张在区内**，不得被底边裁成半张、半张出屏 | 本条不听（布局闸；听感另闸） | 手牌未沉底 / 与牌堆间隙不足 24%；或手牌贴桌心或整排上漂；或手牌整排相对屏幕中线右偏（张数变中心漂移）；或手牌裁半 / 半张出屏；或手牌底缘离底过大 / 悬空偏高 | [ ] |
| **R-LAND-4** | **稳态无大双 CTA** | 稳态桌 **先不点** 出牌 / 质疑。需要时再找弱入口（顶栏 / 侧 / 手牌手势均可） | 没有通栏大双钮「质疑」「出牌」占底。腾出的竖向空间给手牌。弱入口或先藏 **可过**。`lb_btn_challenge` / `lb_btn_play` 锁名保留，可弱可收。**本批不验**完整出牌/质疑流 | 本条不听 | 稳态仍挂大双底栏「质疑」「出牌」 | [ ] |
| **R-LAND-5** | 三座能分人 | **不点**。指 `lb_cmp_seat_p1` / `_p2` / `_p3` + `lb_cmp_seat_self` | **p1=`art_ai_timid_idle` 怂货** · **p2=`art_ai_shark_idle` 老千** · **p3=`art_ai_karen_idle` 杠精**。源 **256 方透明**。靠 id + 显示名能分人。**环位写死**：左 `p3` karen 杠精 · 上 `p2` shark 老千 · 右 `p1` timid 怂货 · **下手 self**。不是方块减号 / 空槽减号 / 占位黑块减号头像 / 三张同一图 | 本条不听 | 未挂锁名 idle；三座分不清；方块减号占位；或环位错（老千不见 / self 跑到中上） | [ ] |

### 3.1 旧号加严（映射，仍可勾）

内容已加严到上表标准。勾「过」时必须同时满足对应 R-LAND。**X3 本批不强制打开。**

| ID | 加严后看什么 | 失败短句 | 过 |
|----|--------------|----------|----|
| **O1** | 进桌立刻横（R-LAND-1）；大厅可继续竖；peek / 出牌包 / 宣称跟 table 横。禁止「先进桌再拧」。**进桌后横屏稳定不闪**（闪烁打回 R-LAND-1） | 先进桌再拧横；或大厅被逼必须横；或进桌后桌面闪烁 / 朝向或布局反复跳动 | [ ] |
| **LY1** | 终稿只用约束 / % / 断点。手牌空隙不写 px。宣称/手牌不进刘海、不贴手势条。**Cover 居中仍有效**：竖底 `art_table_bg` + Cover，锚桌心毡面（约 30%～70% 高），不新出横底 | 硬编码像素当终稿；或宣称/CTA 被刘海裁掉 | [ ] |
| **X1** | 一眼能指四座 + 手牌 + 宣称；**不滚动**整桌。手牌须 **沉底留空**（R-LAND-3）：**手牌贴底安全区**（底缘**只留缝**），与中央牌堆/环心留空约 **24%** 竖向间隙，**禁 padB 顶高**，手牌不得压牌堆/桌心提示，**整张在底安全区**，**水平中心锚固定**（中线=屏幕中线；张数变只左右伸缩、中心不漂；**`selfDock` 不得挤走**）；未沉底 / 间隙不足 24% / 贴桌心或整排上漂 / 中线右偏 / 裁半 / 半张出屏 / 底缘离底过大 / 悬空偏高不过。CTA 以 **稳态无大双 CTA** 为准（R-LAND-4）；弱入口或先藏可过，**不得**再用大双底栏交差 | 要滑才找齐；或缺座/手牌/宣称/CTA 认不出；或手牌未沉底 / 与牌堆间隙不足 24%；或手牌贴桌心或整排上漂；或手牌裁半 / 半张出屏；或手牌底缘离底过大 / 悬空偏高 | [ ] |
| **X2** | **荷官+牌堆**（R-LAND-2）可见，与手牌无致命互盖。`dealPileAnchor` 与池同一中心；bust **禁**被环-手空隙顶飞；池旁张数贴池，不得落到 `lb_cmp_hand` 下；稳态桌心不得再出「第一手还没人出过牌」；仍飘则 **X2 与 D1 一并不过** | 荷官没了 / 只剩空槽；或池旁张数落到手牌下；或荷官被环-手空隙顶飞 / 不在桌心牌堆旁；或 tip 仍在 / 第一手还没人出过牌仍显示 | [ ] |
| **X3** | **本批不强制打开**出牌包、不重做流程。若打开 `lb_sheet_play`：盖住面积 **< 50%**；宣称与四座仍可指 | 出牌包吃掉半屏或以上；或盖住宣称/四座 | [ ] |
| **X4** | 「回开桌」弱于任何出牌/质疑入口（含弱入口）。3～5 分钟主路径不经过。**禁止**用通栏回家填双底栏空档 | 回开桌抢主 CTA；或比质疑/出牌更显眼 | [ ] |

### 3.2 补句（写死）

**R-LAND-1：** 「立刻」= 主钮抬起后叠层第一帧已横。加载只认 `lb_ovl_match_load` + L1a–L1e（动效 + 夜馆垫 + 轮播短提示）。**禁止**另起 `L1f` / 新 `lb_ovl_*`。毫秒窗仍 08，本表不重验数字。  
过关再加严：**进桌后横屏稳定不闪**；朝向只在进桌/离桌设置，禁止稳态轮询反复 `setPreferredOrientation`；禁止 200ms 级无差别 State 全树刷导致可见闪。进桌后桌面闪烁 / 朝向或布局反复跳动 = 失败。根因 ②③（手牌未沉底 / 中心漂移；self 压牌）已由 **R-LAND-3** + [11](../04-设计/11-局内横屏与自适应布局.md) **self 不压牌**覆盖，**不另起闸、不重写那两句**。

**R-LAND-2：** 只用已有 bust 胸像裁切。空 `Image` / 占位框 / 只有池无荷官 = 不过。不要求新全身。池旁张数（工程现挂 `poolText`）须贴 `lb_cmp_pool` / 锚点旁或上，**不得**落到 `lb_cmp_hand` 下；bust 在但池空、张数文案下手牌带 = 仍打回本条。**本版加严：** 荷官 bust **必须**在桌心牌堆旁；加大环-手空隙时 **禁**把 bust 顶飞离 `dealPileAnchor` / `lb_cmp_pool`。荷官被环-手空隙顶飞 / 不在桌心牌堆旁 = 失败。**稳态桌心不得再出现** tip 文案「第一手还没人出过牌」；tip 仍在 / 第一手还没人出过牌仍显示 = 失败。质疑仍冻，不扩质疑流程。

**R-LAND-3：** 对齐 11 §2.1.2 **两闸**（不另起闸）。过关写死：己方手牌沉底可见，**手牌贴底安全区**（底缘贴底安全区上沿，**只留缝**）；与中央牌堆/环心留空约 **24%** 竖向间隙（`HAND_RING_GAP` / 手牌-环隙锁 **24%**）；**禁 padB 顶高**；手牌不得压牌堆/桌心提示；**水平中心锚固定；张数变化只左右伸缩，中心不漂**（中线 = 屏幕/桌面水平中线；**不被 `selfDock` 挤走**）。手牌未沉底 / 与牌堆间隙不足 24% = 失败。手牌贴桌心或整排上漂 = 失败。整排相对屏幕中线右偏（张数变中心漂移）= 失败。11 等价短句可打回：**手牌偏上贴中堆** / **手牌整排右偏**。手牌贴池、或被大底栏顶到池边 = 与 R-LAND-4 一并打回。稳态手牌须 **整张** 落在底边安全区内；被底边裁成半张 / 半张出屏 = 失败（不只「贴中堆」）。**本版加严：** 底缘贴底只留缝；手牌底缘离底过大 / 悬空偏高 = 失败。本条是**布局闸**，不听；听感另闸，**不开 13b**。

**R-LAND-4：** 加严的是 **稳态桌**。不得用本条要求重做质疑四拍。

**R-LAND-5：** 三 id 对得上人格 **且** 不是方块减号。工程在未挂真件前也 **禁止**方块减号当完成。  
**环位写死（对照 11 §2.2 · 不另起闸）：** 左 `p3` karen 杠精 · 上 `p2` shark 老千 · 右 `p1` timid 怂货 · **下手 `lb_cmp_seat_self`**。老千不见 / self 跑到中上 / 占位黑块减号头像 = 不过。

**Cover：** 横裁窗口应吃进竖图中段毡面带。宣称被高对比纹吃掉、或毡心裁飞 = LY1 先改锚，不另出横底。

### 3.3 本表不验（避免误勾）

- 大厅烛火 / 悬停 / 夜馆房间感 / 口播（R1–R4 / G1–G4）——**不重开**
- L1 毫秒、D1 stagger、C1 角标——**仍认 08 / 09**；**终验暂挂**
- 质疑四拍 VFX、砸杯、押面子——**继续冻**
- 出牌包完整改版、带话输入、新主屏——**不做**
- 整应用横屏、大厅五分区改横——**禁止**
- AI 人格概率、`demo_seed`、TC-19、`*_hold` / `*_break`——不扩

---

## 4. 失败短句

勾不通过时只准用下表原句（或等价），不要写成「挤了一点 / 还行」。R-LAND 抄 12 §4（v0.1.7）；O1/X1/X2 用加严句；其余仍抄 10 §4。

| ID | 失败短句（写死） |
|----|------------------|
| R-LAND-1 | 先进桌再拧横；或大厅被逼必须横；或进桌后桌面闪烁 / 朝向或布局反复跳动 |
| R-LAND-2 | 荷官没了 / 只剩空槽；或池旁张数落到手牌下；或荷官被环-手空隙顶飞 / 不在桌心牌堆旁；或 tip 仍在 / 第一手还没人出过牌仍显示 |
| R-LAND-3 | 手牌未沉底 / 与牌堆间隙不足 24%；或手牌贴桌心或整排上漂；或手牌整排相对屏幕中线右偏（张数变中心漂移）；或手牌裁半 / 半张出屏；或手牌底缘离底过大 / 悬空偏高 |
| R-LAND-4 | 稳态仍挂大双底栏「质疑」「出牌」 |
| R-LAND-5 | 未挂锁名 idle；三座分不清；方块减号占位；或环位错（老千不见 / self 跑到中上） |
| O1 | 先进桌再拧横；或大厅被逼必须横；或进桌后桌面闪烁 / 朝向或布局反复跳动 |
| LY1 | 硬编码像素当终稿；或宣称/CTA 被刘海裁掉 |
| X1 | 要滑才找齐；或缺座/手牌/宣称/CTA 认不出；或手牌未沉底 / 与牌堆间隙不足 24%；或手牌贴桌心或整排上漂；或手牌裁半 / 半张出屏；或手牌底缘离底过大 / 悬空偏高 |
| X2 | 荷官没了 / 只剩空槽；或池旁张数落到手牌下；或荷官被环-手空隙顶飞 / 不在桌心牌堆旁；或 tip 仍在 / 第一手还没人出过牌仍显示 |
| X3 | 出牌包吃掉半屏或以上；或盖住宣称/四座 |
| X4 | 回开桌抢主 CTA；或比质疑/出牌更显眼 |

L1a–L1e 失败短句 **仍抄 08 §5.3**，不在上表重复、不在本表勾终验。

---

## 5. 票 B · 牌堆锚点（#48 / `f7c647b` 已合）

本表 **不写像素补丁**。现场只验语义（叠在 R-LAND-2）：

| 锁 | 口径 |
|----|------|
| 视觉落点 | `dealPileAnchor` 叠在 `lb_cmp_dealer` **手边** 或 `lb_cmp_pool` **上方**（08 §2.1 / 09 §1.2） |
| 同一中心 | 所有牌从 **同一锚点** 飞出；锚点中心与牌堆 / 池中心对齐 |
| 仍飘 | 若锚点仍飘在屏外 / 座位脸上 / 与池中心错开 = **R-LAND-2 / X2 与 D1 一并不过** |
| 池旁张数 | 须贴池 / 锚点旁或上。bust 在但池空、`poolText` 落到 `lb_cmp_hand` 下 = **R-LAND-2 / X2 不过**（#58） |
| 荷官禁顶飞 | bust 必须在桌心牌堆旁。被环-手空隙顶飞 / 不在 `dealPileAnchor` 旁 = **R-LAND-2 / X2 不过** |
| 删首出 tip | 稳态桌心不得再出现「第一手还没人出过牌」。tip 仍在 = **R-LAND-2 / X2 不过** |

票 B 已合 **≠** 本批自动过关。未 Rebuild 到含打回加严桌的包，**不得勾 R-LAND-2**，更不得提前开 D1 终验。

---

## 6. 手牌 UX 不变（C1 仍暂挂）

本表 **不改** GDD §3.4 / 08 / 09。横屏只改桌面怎么排。

| 锁 | 口径 |
|----|------|
| 飞行 / 落座稳态 | **牌背**（自己 `lb_cmp_hand` 亦然） |
| 对手 | **始终背** |
| 揭面 | **仅** 长按 peek 及出牌包选牌 |
| C1 | **只验揭开后** 角标；**终验仍暂挂** |
| 横屏 | **不**改成稳态明牌、不改长按手势 |

---

## 7. 不算 / 不做

| 不算 / 不做 | 说明 |
|-------------|------|
| **合入文档 / 合入客户端当终验** | 合入 ≠ 过关；**等 Rebuild 现场过再勾** |
| **只过旧 O1/X1 勾本批** | 本批硬门槛是 R-LAND-1～5 |
| **质疑四拍 VFX / 仪式** | 继续冻；不提前验收 `lb_scr_challenge` |
| **大厅重做 / 强制横 / 重开 G1–G4 / R1–R4** | lobby **可以**竖。整应用必须横 = R-LAND-1 / O1 失败。进桌后桌面闪烁 / 朝向或布局反复跳动 = R-LAND-1 失败 |
| **另起加载闸 / 重验 L1 毫秒** | 只认 `lb_ovl_match_load` + L1a–L1e；终验仍暂挂 |
| **荷官新全身 / 新 bust 文件名** | 只用已有 `art_dealer_bust_*` 裁切 |
| **方框减号 / 占位黑块减号当完成** | R-LAND-5 不过（含环位错：老千不见 / self 跑到中上） |
| **改手牌 UX / C1 / 规则数字** | 稳态背 + 长按 peek；张数/命数/Joker 不动 |
| **本批验完整出牌 / 质疑流程** | 弱入口或先藏可过 R-LAND-4 |
| **新开 13b / 把听感并进 R-LAND-3** | 布局闸与听感分闸。本条不听；13b 换源另票 |
| **扩桌布 / 牌面 / 质疑图** | Cover 居中仍有效；不新出 `art_table_bg_land` |

---

## 8. 锁名摘录（步骤只引用这些）

| id | 层 | 本表怎么用 |
|----|-----|------------|
| `lb_scr_lobby` | 主屏 | **可继续竖**（R-LAND-1 / O1） |
| `lb_ovl_match_load` | 叠层 | **复用 L1 加载**；第一帧已横。禁「加载跟 lobby 竖」 |
| `lb_scr_table` | 主屏 | **强制横**。切到时已经是横，用户不必再拧。**进桌后横屏稳定不闪** |
| `lb_cmp_dealer` | 荷官 | R-LAND-2：已有 `art_dealer_bust_idle` / `_announce`；须在桌心牌堆旁；**禁**被环-手空隙顶飞 |
| `lb_cmp_pool` / `dealPileAnchor` | 牌堆 | 必须保留；bust 在旁或上；池旁张数贴池；稳态无 tip「第一手还没人出过牌」 |
| `lb_cmp_hand` / `lb_cmp_card_{0-4}` | 手牌 | R-LAND-3：再沉底 + 环隙锁 **24%** + 水平中线锚；**贴底安全区只留缝**；**禁 padB**；与中央牌堆/环心留空约 **24%**；中线=屏幕中线；**`selfDock` 不得挤中心**；**整张在区内**；稳态牌背 |
| `lb_cmp_seat_p1` | 座 | **右** · **怂货** · `art_ai_timid_idle` |
| `lb_cmp_seat_p2` | 座 | **上** · **老千** · `art_ai_shark_idle` |
| `lb_cmp_seat_p3` | 座 | **左** · **杠精** · `art_ai_karen_idle` |
| `lb_cmp_seat_self` | 座 | **下** · 系统头；不挂 `art_ai_*` |
| `lb_btn_challenge` / `lb_btn_play` | CTA | 锁名保留；稳态可弱/隐（R-LAND-4） |
| `lb_txt_claim` | 宣称 | 不得进刘海（LY1） |
| `art_table_bg` | 桌布 | 竖底 + Cover 居中仍有效 |
| `lb_scr_challenge` | 主屏 | **本批继续冻** |

操作点 `lb_*`。媒体核对 `art_*`。**禁止** `lb_art_*`。不得升格第五主屏、不得新造 overlay id。

---

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-10 | #50 打回后策划 #52 / UI #53 / 美术 #54 已齐；测试先起草勾选，等客户端合入后再验 | 旧 #49 表加严到 R-LAND-1～5；合入≠终验；L1/C1/D1 仍暂挂 | 测试岗 | **v0.2.0 已写入** |
| 2026-09-10 | #58 / `81bf7c3` 合入 12 v0.1.3：失败短句补裁半、环位、池旁张数下手牌 | 勾选表 R-LAND-2/3/5（及 X1/X2）抄 §3/§4 原句；不另起闸 | 测试岗 | **v0.2.1 已写入** |
| 2026-09-10 | 12 v0.1.4：R-LAND-3 对齐 11 v0.2.4 两闸。失败短句写死「手牌仍靠近桌心 / 未沉底」「整排相对屏幕中线右偏（张数变中心漂移）」；过关写死贴底安全区 + 水平中心锚（`selfDock` 不得挤中心） | 勾选表 R-LAND-3（及 X1 看什么）抄 12 §3/§4 原句；贴中堆 / 裁半仍打回；不另起闸 | 测试岗 | **v0.2.2 已写入** |
| 2026-09-10 | 12 v0.1.5：R-LAND-1 补失败短句「进桌后桌面闪烁 / 朝向或布局反复跳动」。过关写死进桌后横屏稳定不闪；朝向只在进桌/离桌；禁稳态轮询反复 `setPreferredOrientation` / 200ms 级无差别 State 全树刷 | 勾选表 R-LAND-1（及 O1）抄 12 §3/§4 原句；根因 ②③ 认 R-LAND-3 + 11 self 不压牌，不另起闸 | 测试岗 | **v0.2.3 已写入** |
| 2026-09-10 | 12 v0.1.6：R-LAND-3 手牌-环隙锁 **24%**（`HAND_RING_GAP`；原环隙数字让位）。失败短句写死「手牌未沉底 / 与牌堆间隙不足 24%」「手牌贴桌心或整排上漂」。过关写死沉底可见、与中央牌堆/环心留空约 24%、不得压牌堆/桌心提示 | 勾选表 R-LAND-3（及 X1 看什么）抄 12 §3/§4 原句；布局闸与 BGM 听验分闸；不开 13b；不改 11；不另起闸 | 测试岗 | **v0.2.4 已写入** |
| 2026-09-11 | 12 v0.1.7：Rebuild 三闸挂 R-LAND-2/3。失败短句写死「手牌底缘离底过大 / 悬空偏高」「荷官被环-手空隙顶飞 / 不在桌心牌堆旁」「tip 仍在 / 第一手还没人出过牌仍显示」。仍认 GAP=24% / 禁 padB | 勾选表 R-LAND-2/3（及 X1/X2）抄 12 §3/§4 原句；布局闸与听感分闸；不开 13b；不改 11；不另起闸 | 测试岗 | **本 PR 写入 · 待 @LiarBar负责人 审** |

请 **@LiarBar负责人** 审本清单：Rebuild 三闸失败短句是否一字对齐 12 v0.1.7（手牌底缘离底过大 / 悬空偏高；荷官被环-手空隙顶飞 / 不在桌心牌堆旁；tip 仍在 / 第一手还没人出过牌仍显示）；过关是否写死手牌贴底只留缝、荷官在桌心牌堆旁、稳态无首出 tip；仍认 GAP=**24%** / **禁 padB**；布局闸与听感是否分闸、13b 是否未开；合入≠终验、L1/C1/D1 暂挂、质疑仍冻是否仍在。

---

*测试岗 · 抄 12 R-LAND-1～5（#52 / `f9f8c0c`）· 失败短句 12 v0.1.7（本 PR · Rebuild 三闸）· 11 对照词（#53 / `4897657`）· 三 idle #54 / `a74eebdc` · 旧 #49 O1/LY1/X1–X4 映射加严 · 合入≠终验 · 等客户端 Rebuild 现场过再勾 · L1/C1/D1 仍暂挂 · 质疑仍冻 · Cover 居中仍有效 · 不改规则 · 不开 13b*
