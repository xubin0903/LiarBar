# 开场与大厅验收勾选清单

> ## 验收暂停过关 · 待烛火 / 悬停 / 声场返工
>
> **对照用户打回（当前构建）：** 烛火是方框贴片、无悬停闪动、BGM/氛围差、口播干巴复读机。  
> **本表不是可过关清单。** 口径仍按 v3 记；**当前构建不得宣称 v3 过关。**  
> 返工 PR（美术 candle / VO / BGM·amb、UI hover 规格、鸿蒙落码）**合入后再验**。未满足下方过关闸，**不得勾「过关」**。  
> 局内联合验（**TC-19**）**继续挂起**。

| 项 | 内容 |
|----|------|
| 版本 | **v3 · 验收暂停过关**（取代 #24 的 1.5–2.5s 窗；**不**等于现构建已过） |
| 读者 | 评委 / 制作人 / **用户本机终验** / 返工岗 |
| 目标 | 对齐 [`04` §7](../04-设计/04-开场声场与大厅氛围-v3.md) 记现象；默认 **3.8s（3.0～4.5）** + 五层声场 + 荷官真动作。**过关另见 §1 闸门** |
| 对齐 | PR#26/`12d74bb` · #27/`e535953` · #28/`6034132` · #29/`26e0c70` · **负责人打回（烛火/悬停/声场）** |
| 范围 | **只记** 打开 App + `lb_scr_lobby`。局内联合验（**TC-19**）**继续挂起** |
| 配套 | [测试用例.md](./测试用例.md) **TC-01** · [大厅氛围v3-资产交件.md](../04-设计/大厅氛围v3-资产交件.md) · [05-开场大厅v3-UI动效挂点.md](../04-设计/05-开场大厅v3-UI动效挂点.md) |

> **本表取代 #24 的 1.5–2.5s 窗。** 旧默认 ~2.0s、clamp 1.5–2.5s **作废**。  
> 画面分层、独立静帧、五分区、锁句文字仍对齐 [`03` §7](../04-设计/03-开场与大厅交互-v2.md)；**时刻与听感以 `04` §7 / 本表为准**（默认 **3.8s**）。  
> **本表不是局内演示清单。** 不扩 `lb_scr_table` / 质疑四拍 / `demo_seed` / 规则。  
> **TC-01 冷启动进大厅仍算进厅门槛**；进得了大厅 **≠** v3 过关。

---

## 0. 当前结论（先读 · 不得宣称过关）

| 项 | 口径 |
|----|------|
| **现构建** | 含 #29（`26e0c70`）及以前的 v3 落地。**不得宣称 v3 过关** |
| **打回点** | 烛火方框贴 · 无悬停闪动 · BGM/氛围差 · 口播干巴复读机 |
| **何时再验过关** | 美术（candle / VO / BGM·amb）+ UI hover 规格 + 鸿蒙落码 **返工 PR 合入之后** |
| **本表表格末列** | 只勾 **「记」**（看见 / 听见什么）。**「过关」只在 §1 闸门，现全部不得勾** |
| **局内** | TC-19 **继续挂起** |

---

## 1. 过关闸（加严 · 未满足不得勾「过关」）

下列 **四条同时满足** 才允许讨论 v3 过关。任一条仍是打回状 = **过关暂停**。  
现构建按打回记录：**四条均未过，全部保持未勾。**

| # | 闸门 | 必须看到 / 听到 | 打回对照（现构建） | 过关 |
|---|------|-----------------|---------------------|------|
| G1 | **烛火融进景深** | 近景烛焰像馆里的火：吃进 `art_lobby_bg` 景深 / 近烛位，焰芯闪、不闪整屏 | **方框贴片**（能看出一张矩形贴上去）= 不过 | [ ] **不得勾** |
| G2 | **CTA 可见闪动** | `lb_btn_quickstart` 在 **悬停 / focus** 须有肉眼可见闪动（铜边 / 烛光高光，不是只 `scale`）。真机无悬停：用 **focus 或 pressed** 兜底，按下仍须闪一下 | **无悬停闪动** = 不过。只有整钮缩放、无光感变化 = 不过 | [ ] **不得勾** |
| G3 | **房间感声场** | `bgm_lobby_night` + `sfx_amb_tavern` 能分开指认，且像 **夜深还开着的酒馆**（低弦托底 + 极轻杯碰/人声/木梁） | **弱垫 / beep / 同一条短波充两层** = 不过。有分层但「氛围差、不像房间」= 不过 | [ ] **不得勾** |
| G4 | **口播有情绪** | `vo_dealer_greet` 锁句仍是 **「夜深了，位子给你留着。」**；须有近距、略沙、像对人说话的 **情绪**，与字幕同起 | **干巴复读机** / TTS 念稿无语气 / 蜂鸣代口播 = 不过。改锁句 = 不过 | [ ] **不得勾** |

返工未合入前，**禁止**把下表「记」勾满后写成「v3 已过」。

---

## 2. 前置（不齐不记现象）

- [ ] 本机 DevEco 已 **Rebuild**。现基线含 **#29**（`26e0c70`）；**过关复验**须再 Rebuild 到含烛火 / hover / 声场返工 PR 的包（不要热重载冒充冷启动）
- [ ] 真机从 **桌面点图标** 冷启；验的是同一路由 `lb_scr_lobby`（不另开 `lb_scr_home`）
- [ ] 锁名能对上：`lb_scr_lobby` / `lb_btn_quickstart` / `lb_cmp_dealer` / `lb_tog_silent` / **独立** `art_splash_still`
- [ ] 五层媒体能对上：`bgm_lobby_night` / `sfx_amb_tavern` / `sfx_boot_hit` / `sfx_cta_tap` / `vo_dealer_greet`（v2 占位 `sfx_boot` / `sfx_lobby_amb` / `sfx_cta` **不得当交件**）
- [ ] 荷官四态能对上：`art_dealer_idle_blink` / `art_dealer_idle_nod` / `art_dealer_idle_cup` / `art_dealer_idle_mask`
- [ ] **局内联合验（TC-19）继续挂起**；本表打完「记」≠ 课设 5 分钟局内过关，更 **≠** v3 过关
- [ ] **TC-01** 仍须能冷启动进 `lb_scr_lobby`（崩溃 / 进不了大厅 = 连现象都记不成）

---

## 3. 硬口径（v3 仍有效 · 过关另看 §1）

H1 / H2 / H3 与加长轴 **继续有效**，用来记现象、卡返工。**满足本表 ≠ 现构建过关。**

| 口径 | 要记到什么 | 记不过 / 过关仍停 |
|------|------------|-------------------|
| **H1 听 / 看 / 点** | 下表每一行三列齐全；先看后点 | 只有「听到 xx」或只有画面、没写点或不点 |
| **H2 禁 beep** | 五层是 **五条不同材质**：BGM / 环境 / 开场击点 / CTA / 口播 | 单 beep / 方波 / 正弦 tip；三槽同一条短波 |
| **H2 禁仅缩放** | 荷官有 **真动作循环**：眨眼 +（点头 / 抬杯 / 面具）至少一种 | 大厅只有 `scale` 呼吸交差 |
| **H3 静默全关** | 开 `lb_tog_silent` → 杀进程 → 桌面重开：口播 / 强震 / BGM / 环境 / 开场击点 / CTA **五层全关**；**字幕仍出**锁句 | 静默后馆里还在响；或字幕没了 |
| **烛火（G1）** | 焰 **融进景深**，不是方框贴片 | 能描出烛图层矩形外框 |
| **CTA 闪动（G2）** | 悬停 / focus 可见闪；真机用 focus / pressed 兜底 | 无光感变化，只缩放 |
| **房间感（G3）** | BGM + 环境像酒馆房间，不是弱垫 | 氛围差、弱垫、beep |
| **口播情绪（G4）** | 锁句 + 有情绪，非干巴复读 | 复读机 / 改字 |
| **独立静帧** | `art_splash_still` ≠ `art_lobby_bg` | 同一张褐金底糊开场 |
| **冷启动总长** | 落在 **3.0～4.5s**（默认 **3.8s**） | 仍按 1.5–2.5s；短于 3.0s 或长于 4.5s；删拍 |

时刻按默认 **3.8s** 轴书写。总长若调到 3.0s / 4.5s，**拍顺序不变**，只按比例伸缩画面段。  
轴未到 T6（默认 3.8s）前，主 CTA **不可点**。「跳过开场」= P1，本批不强制。  
口播禁止变速 / 剪句（自然语速 1.8～2.4s）；口播可溢入稳态，**不挡** T6 可点。

### 3.1 五层声场（须能分开指认 · G3 / G4 加严）

| 层 | 文件 | 听感（过关要这个） | 出现 | 打回不过 |
|----|------|-------------------|------|----------|
| BGM | `bgm_lobby_night` | 低、慢、暖的夜馆曲子，有房间托底 | T2 淡入 → 稳态循环 | 弱电子垫 / 2s 短 loop 接缝 / beep |
| 环境 | `sfx_amb_tavern` | 极轻杯碰 / 远处人声 / 木梁，单听像房间 | T2 淡入 → 稳态循环 | 空垫、广告闲聊、与 BGM 分不开 |
| 开场击点 | `sfx_boot_hit` | 一记招牌 / 灯亮（铜撞 + 短厅响），**不是 beep** | T1 ~0.18s | 系统叮 / 方波 |
| CTA | `sfx_cta_tap` | 短木质 / 铜钉；按下 80ms 内必须听到 | **仅用户按下之后** | 方波 beep |
| 荷官招呼 | `vo_dealer_greet` | 锁句 + **有情绪**（近距、略沙，像对人说） | T4 2.20s ±80ms | **干巴复读机** / 改锁句 |

---

## 4. §7.1 前置（点 / 看 / 听 / 记）

**先看后点。** 末列只记现象。勾「记」**不等于**过关。

| # | **点什么** | **看什么** | **听什么** | 记 |
|---|------------|------------|------------|----|
| P0 | **不点**厅内。确认构建版本（现 #29；复验须含返工 PR） | 锁名能对上：`lb_scr_lobby` / `lb_btn_quickstart` / `lb_cmp_dealer` / `lb_tog_silent` / `art_splash_still` | 未开验前不应有厅内循环声抢跑 | [ ] |
| P1 | 从 **桌面点图标** 冷启（不要热重载冒充） | 进的是同一路由 `lb_scr_lobby`，不是 `lb_scr_home` | 冷启瞬间无残响、无上一局 BGM | [ ] |
| P2 | **不点** Table / 质疑 / 战报当本表验收 | 局内 / `demo_seed` / TC-19 **不扩验** | 本表不验收局内任何音 | [ ] |

---

## 5. §7.2 开场声场与加长轴

时刻按默认 **3.8s**。#24 的 1.5–2.5s **不再作为时长门槛**。

| # | **点什么** | **看什么** | **听什么** | 记 |
|---|------------|------------|------------|----|
| 0 | **不点**。先看完开场再对比 | 独立静帧 ≠ 大厅景深底；`lb_cmp_dealer` 有层次；CTA 有质感 | 能指认击点 / BGM / 环境 / 口播 **不是同一条 beep**。BGM+环境须像房间（G3） | [ ] |
| 1 | **打开 App**（桌面点图标）。手离开，**不点**厅内按钮 | 黑场 `tavern_bg` `#1A120E`。不是立刻褐卡表单 | **无声**。无 BGM 抢跑 | [ ] |
| 2 | **不点** | 独立 `art_splash_still` 从黑场淡入，无闪白 | **~0.18s** 听到 `sfx_boot_hit`（招牌/灯亮，有厅响）。**不是 beep** | [ ] |
| 3 | **不点** | 加载暗示（转圈或底沿细条）叠在静帧上 | **0.6～1.4s** 能分开指认 `bgm_lobby_night` 与 `sfx_amb_tavern`，且有房间感（G3） | [ ] |
| 4 | **不点**。此拍若误点 `lb_btn_quickstart` 也不该开桌 | `lb_cmp_dealer` idle 胸像出现；景深底与静帧可并排区分 | BGM + 环境继续。**无**口播、**无**新击打 | [ ] |
| 5 | **不点**。眼看 `lb_cmp_dealer` 旁字幕 | 荷官切 announce；字幕为 **「夜深了，位子给你留着。」** | **2.20s ±80ms** `vo_dealer_greet` 与字幕同起；BGM duck。口播 1.8～2.4s 说完，**须有情绪**（G4），非干巴复读 | [ ] |
| 6 | **不点** | 设置卡 → 主 CTA →「进桌看看」错开入场 | **无**额外 beep / stagger 击打 | [ ] |
| 7 | **先不点**。看整屏，确认轴已结束（不得早于 3.0s、不得晚于 4.5s；默认 3.8s） | 五分区可辨。烛火须已融进近景（G1），不是方框贴 | 口播若未完可继续；无新击打。非静默时 BGM/环境仍在且像房间 | [ ] |
| 8 | **悬停或 focus `lb_btn_quickstart`**（真机无悬停则 focus；再试 pressed） | **须可见闪动**（G2）。再点：整钮缩放 ≤80ms；进入默认开桌（规则不变） | **≤80ms** 听到 `sfx_cta_tap`（木质/铜钉）。**不是**方波 beep。静默则无击打 | [ ] |
| 8b | 回大厅稳态后 **点次级「进桌看看」**（不要点成主 CTA） | 弱入口、不抢主按钮；能进桌预览。**局内画面不在本表验收** | 同一 `sfx_cta_tap` **更轻（−3 dB）**。不是第二条不同 beep | [ ] |

---

## 6. §7.3 大厅稳态（停 ≥8s）

T6 之后（或口播回 idle 之后，取晚者）自动开循环，**无需玩家点荷官**。

| # | **点什么** | **看什么** | **听什么** | 记 |
|---|------------|------------|------------|----|
| 9 | **不点** `lb_cmp_dealer`。停在大厅 ≥8s，只盯荷官 | **真动作循环**：8s 内至少一次 **眨眼**，以及点头 / 抬杯 / 面具微动 **至少一种**（`art_dealer_idle_{blink,nod,cup,mask}`）。**仅 `scale` 呼吸 = H2 不过** | 非静默：BGM 稳态 + 环境继续，须仍有房间感（G3）。无新击打、无第二句口播 | [ ] |
| 9b | **不点** | 近景烛焰 **融进景深** 在闪（焰，不是整屏闪，**禁止方框贴片**，G1）；远景烟或光尘微动 | 无额外 FX 音。禁止为烛光加 beep | [ ] |
| 9c | **悬停 / focus `lb_btn_quickstart`**（真机无悬停用 focus / pressed 兜底）。本条不要求开桌 | CTA **可见闪动**（G2）。入场后无大弹跳；稳态微光 scale ≤1.01 | 悬停本身无新击打；按下才有 `sfx_cta_tap` | [ ] |
| 9d | **不点**。从 T4 看到 T6 之后 | 口播期间是 announce；口播结束后回 idle，再进真动作循环，不是一直举牌 | 口播结束后 BGM **200ms 回稳态**；无第二句口播。口播本身须过 G4 | [ ] |

---

## 7. §7.4 静默局（H3 写死）

| # | **点什么** | **看什么** | **听什么** | 记 |
|---|------------|------------|------------|----|
| 10 | 回大厅后 **点 `lb_tog_silent` 打开** → 杀进程 → **从桌面再点图标**重开（再走 3.0～4.5s） | 招呼 **字幕仍在**，句为「夜深了，位子给你留着。」荷官真动作与 **融进景深的烛火** 仍在 | **全静**：口播关、强震关、开场击点关、CTA 击打关、**BGM 关、环境关**。无「留低音量」备选 | [ ] |
| 10b | 静默重开、轴结束后 **focus / pressed 再点 `lb_btn_quickstart`** | 整钮仍缩放；**仍须可见闪动**（G2，静默不是冻光感）；仍进默认开桌（规则不变） | **无** `sfx_cta_tap`、**无**强震 | [ ] |

---

## 8. 分层画面条（#22 真图保留 · 时刻以 3.8s 为准 · 只记）

能用手指认才勾「记」。不是「屏幕上有这些色就过关」。时长仍按 3.8s 轴。

| 层 | 应能指认 | token（对照，不是填色过关） | 记 |
|----|----------|------------------------------|----|
| 远景 | 冷雾 / 四角暗 | `tavern_shadow` `#0E1A22` | [ ] |
| 中景 | 木纹 / 桌沿 / 招牌木 | `tavern_panel` `#3D241C` | [ ] |
| 近景烛 | 焰 **融进** 近烛位（G1）。`art_fx_candle` 只闪焰 | `tavern_candle` `#E8B86D`。**方框贴片 = 记不过 / 过关停** | [ ] |
| 荷官 | 侧光 / 面具 / 肩；idle↔announce 有层次；稳态能认出四态之一 | 真 bust + `art_dealer_idle_*`，不是平涂、不是只缩放 | [ ] |
| 独立静帧 | **`art_splash_still`** 与 `art_lobby_bg` 可并排对比 | 两张不是同一张糊图 | [ ] |
| CTA | `lb_btn_quickstart` 从景深跳出；**悬停/focus/pressed 须闪**（G2） | `tavern_accent` `#F0C14B`。只缩放无闪 = 过关停 | [ ] |

同一张褐金底糊开场、或只有 token 色块拼贴 = 画面记不过。画面像样、听感仍是弱垫 / 复读机 = **G3 / G4 过关停**。

---

## 9. 失败短句（出现即不得过关）

**只记开场+大厅**，不记局内输赢。现构建已命中打回点，**过关暂停**。

- **验收暂停过关**：返工 PR 未合入，仍宣称 v3 过关。
- 烛火是 **方框贴片**，未融进景深（G1）。
- `lb_btn_quickstart` **无悬停 / focus / pressed 可见闪动**（G2）。
- BGM / 环境是弱垫、beep，或没有房间感（G3）。
- 口播是 **干巴复读机**，或改了锁句（G4）。
- 验收句缺 **听 / 看 / 点** 任一列（H1）。
- 冷启动进不了 `lb_scr_lobby`（TC-01 门槛失败）。
- 总轴短于 3.0s 或长于 4.5s；或删了黑场 / 静帧 / 加载 / 荷官 / stagger / 稳态 / 声场起层任一段。
- 仍按 #24 的 **1.5–2.5s** 当时长门槛。
- **单 beep 冒充完成**；三槽同一条短波；无分层 BGM / 环境 / 口播（H2）。
- 大厅 **仅缩放交差**；无眨眼 +（点头 / 抬杯 / 面具）真动作循环（H2）。
- 无独立 `art_splash_still`，或开场静帧就是 `art_lobby_bg`。
- 口播与字幕不同起。
- 静默局仍有 BGM / 环境 / 击打 / 口播 / 强震；或字幕没了（H3）。
- 轴未完点 `lb_btn_quickstart` 开了桌；或轴完后主 CTA 仍不可点。
- 「进桌看看」做成第二个主 CTA。
- 未审先落占位音 / 占位动效当通过。
- 改了局内 / 规则 / 锁名 / `lb_art_*`。
- 把本表扩成局内 / `demo_seed` / TC-19 联合验。

---

## 10. 本表不算 / 不做

| 不算 | 原因 |
|------|------|
| **宣称现构建 v3 过关** | **暂停过关**；待烛火 / 悬停 / 声场返工合入 |
| TC-19 `demo_seed` 90s 冒烟 | **继续挂起**；不在本表范围 |
| 质疑四拍 / 战报 / 看牌防窥 | 局内联合验，冻结 |
| 改规则 / 引擎 / ets / 锁名 | 测试岗只交清单；规则 v0.2 不动。返工由美术 / UI / 鸿蒙另开 PR |
| 「跳过开场」 | P1，本批不强制 |
| 用热重载 / 截一张稳态图代替冷启动 | 必须从桌面重开，按拍看 |
| 从桌回大厅再走 3.8s 整轴 | 冷启动只冷启一次；回厅 0.4s 短入，不重播击点 / 招呼 |

---

## 11. 锁名摘录（步骤只引用这些）

| id | 层 | 本表怎么用 |
|----|-----|------------|
| `lb_scr_lobby` | 主屏 | 冷启动 + 大厅，同一路由 |
| `lb_btn_quickstart` | 主 CTA | 默认 3.8s 后才点；**悬停/focus/pressed 须闪**（G2） |
| `lb_cmp_dealer` | 荷官区 | 层次 + 招呼字幕 + 四态循环 |
| `lb_tog_silent` | 设置卡 | #10 开静默后杀进程、桌面重开 |
| `art_splash_still` | 媒体 | **独立**启动静帧；禁止 `lb_art_*` |
| `art_fx_candle` | 媒体 | 近景焰；必须融进景深，禁方框贴（G1） |
| `art_dealer_idle_blink` / `_nod` / `_cup` / `_mask` | 媒体 | 荷官真动作四态 |
| `lb_btn_peek_table` | 次级（建议） | 「进桌看看」；弱于主 CTA |
| `lb_bgm_lobby_night` | 调用点 | BGM；须有房间感（G3） |
| `lb_sfx_amb_tavern` | 调用点 | 环境；须有房间感（G3） |
| `lb_sfx_boot_hit` | 调用点 | 开场击点 |
| `lb_sfx_cta_tap` | 调用点 | CTA 击打 |
| `lb_vo_dealer_greet` | 调用点 | 招呼口播；须有情绪（G4） |

可出现：`lb_str_dlr_lobby_greet`、`lb_cmp_nick_field`、`lb_cmp_playercount`。  
操作点 `lb_*`；媒体 `art_*`；音频 `bgm_*` / `sfx_*` / `vo_*`。**禁止** `lb_art_*`。

废止当完成：`sfx_boot` · `sfx_lobby_amb` · `sfx_cta` 及 `lb_sfx_boot` / `lb_sfx_lobby_amb` / `lb_sfx_cta`。

---

*测试岗 · 验收暂停过关 · 待烛火/悬停/声场返工 · 对齐负责人打回 · 不改规则 · 局内 TC-19 继续挂起*
