# 《谎馆》数值与牌堆配置

| 项 | 内容 |
|----|------|
| 版本 | **v0.2 制作人锁定** |
| 状态 | 规则相关键已锁定 · 手感项仍可试玩调表 |
| 原则 | 所有数进配置表，禁止写死魔法数在业务逻辑各处 |

---

## 1. 对局基础

| 键 | 默认值 | 说明 |
|----|--------|------|
| `min_players` | 3 | 含 AI |
| `max_players` | 6 | MVP 主做 4 |
| `default_players` | 4 | 快速开桌 |
| `lives_default` | 3 | 命（已锁定） |
| `hand_size_default` | 5 | 每人手牌（已锁定） |
| `max_play_cards` | 3 | 单次最多出（已锁定） |
| `min_play_cards` | 1 | 单次最少出（已锁定） |
| `turn_seconds` | 15 | 出牌/决策时限 |
| `challenge_only_seconds` | **8** | 无手牌且仅可质疑时的专用时限（§10.1） |
| `challenge_bet_seconds` | 2 | 旁观押真假窗口 |
| `challenge_ritual_max_seconds` | 4 | 三拍总长（可跳过） |
| `low_time_threshold` | 5 | 开始可「催」 |
| `timeout_auto_play` | true | 超时自动出 1 张 |
| `max_slam_per_game` | 5 | 甩出次数上限/局 |
| `max_hesitate_per_game` | 8 | 犹豫拍上限/局（可放宽） |
| `hesitate_dwell_ms` | **800** | HESITATE 判定：拖拽停顿达到该毫秒数（= **0.8s**） |
| `soft_max_velocity` | **1200** | SOFT 判定：滑动速度上限，单位 **px/s**；≤ 此值记为 SOFT |
| `slam_min_velocity` | **2800** | SLAM 判定：滑动速度下限，单位 **px/s**；≥ 此值记为 SLAM |
| `demo_seed_enabled` | **true** | 演示种子局默认开 |
| `demo_seed_value` | **20260906** | 固定 seed；配合种子剧本，约 **90s 内必现一次完整质疑**（指控→对峙押注→翻牌结算） |

---

## 2. 牌堆

### 2.1 符号集合

```
RANKS = [A, K, Q]
WILD = JOKER
CLAIMABLE = [A, K, Q]   // Joker 不可作宣称目标（已锁定）
```

工程 rawfile/deck.json 运行时键名为 n3..n6，人数语义仍为 3～6 人桌；各袋张数不变。

### 2.2 默认 4 人桌构成

| 牌 | 张数 |
|----|------|
| A | 8 |
| K | 8 |
| Q | 8 |
| JOKER | 4 |
| **合计** | **28** |

发牌：`4 × 5 = 20`，剩余 8 张弃置本局不用。  
手牌耗尽触发重建发牌时：对存活玩家按 `hand_size_default` 从**完整牌堆**重新洗牌重发（见 GDD §10.1）。

### 2.3 3 人桌建议

| 牌 | 张数 |
|----|------|
| A | 6 |
| K | 6 |
| Q | 6 |
| JOKER | 3 |
| 手牌 | 每人 5（发 15） |

### 2.4 5～6 人桌建议（P1）

| 人数 | 每符号 | Joker | 手牌 |
|------|--------|-------|------|
| 5 | 10 | 5 | 5 |
| 6 | 12 | 6 | 5 |

### 2.5 宣称随机

- 均匀抽自 `CLAIMABLE`  
- `avoid_same_claim_streak: 1`（尽量不与上轮相同）  

### 2.6 真假判定

```
is_legal(card, claim) =
  card.rank == claim  OR  card.rank == JOKER

hand_is_clean(cards, claim) =
  ∀c ∈ cards: is_legal(c, claim)

// 质疑成功 ⇔ NOT hand_is_clean(被质疑本手, claim)
```

---

## 3. 面子分（旁观押注）

| 键 | 默认 | 说明 |
|----|------|------|
| `face_bet_enabled` | true | **默认开**（已锁定） |
| `face_bet_correct` | +2 | |
| `face_bet_wrong` | **0** | 不惩罚，降焦虑（已锁定） |
| `face_score_display` | 局后+小徽章 | 对局中可弱显示 |

---

## 4. 互动次数上限（每回合 / 每局）

| 动作 | 每回合 | 每局 |
|------|--------|------|
| KNOCK（含三连强化） | 2 | 30 |
| STARE | 1 | 20 |
| SIGN_OK / SIGN_NO | 各 2 | 40 |
| HECKLE | 2 | 40 |
| 点名（出牌时） | 1（随出牌） | — |
| 催 | 1（仅 low_time） | — |

---

## 5. AI 牌友概率表（MVP）

> 值域 0～1。程序按人格读表。试玩只改表不改代码。

### 5.1 出牌

| 人格 | `p_play_truth_when_can` | `p_prefer_slam` | `p_prefer_hesitate_bluff` | `avg_play_count_bias` |
|------|-------------------------|-----------------|---------------------------|------------------------|
| 怂货 TIMID | 0.85 | 0.10 | 0.15 | 偏 1 张 |
| 老千 SHARK | 0.45 | 0.35 | 0.25 | 偏 2 张 |
| 杠精 KAREN | 0.55 | 0.25 | 0.20 | 偏 1～2 |

**出假时选牌**：优先丢非宣称非 Joker；若要掺真，保留 Joker 作后续。

### 5.2 质疑

| 人格 | `base_p_challenge` | 上家连续出牌 +1 手时加成 | 上家 SLAM 时加成 | 手牌很强时（真牌≥3）抑制 |
|------|--------------------|--------------------------|------------------|---------------------------|
| 怂货 | 0.12 | +0.05 | +0.05 | -0.05 |
| 老千 | 0.28 | +0.08 | +0.10 | -0.02 |
| 杠精 | 0.42 | +0.10 | +0.12 | 0 |

最终 `clamp(p, 0.05, 0.85)`。

### 5.3 表演

| 人格 | 出牌后 emote | 敲桌 | 画✗ | 点名 |
|------|--------------|------|-----|------|
| 怂货 | 0.2 | 0.05 | 0.05 | 0.05 |
| 老千 | 0.5 | 0.15 | 0.15 | 0.25 |
| 杠精 | 0.45 | 0.25 | 0.35 | 0.20 |

### 5.4 AI 决策时限

- 思考表现延迟：`ai_think_ms = 400~1200` 随机，显得像人  
- 上限不超过 `turn_seconds - 2`  

---

## 6. 超时与托管

| 键 | 默认 |
|----|------|
| `auto_play_prefer_legal` | true（有真优先出真 1 张） |
| `auto_play_else` | 随机 1 张 |
| `consecutive_timeout_warn` | 2 |
| `challenge_only_on_timeout` | skip（不自动质疑） |

---

## 7. 目标手感（调参方向）

| 现象 | 调什么 |
|------|--------|
| 局太长 | ↓手牌 或 ↓命 或 ↑质疑率（AI） |
| 假牌太易被抓 | ↑Joker 或 ↑手牌真牌密度 |
| 没人敢质疑 | ↑`base_p_challenge` 或 ↑回合时间压迫 |
| AI 太蠢好欺负 | ↑老千比例；↑ SHARK `p_play_truth` 波动 |
| 演示走不满一次质疑 | 开“教学剧本种子局”（P1 调试功能） |
| 无牌卡死感 | 已由 §10.1 + `challenge_only_seconds` 覆盖 |

### 7.1 演示种子局（建议程序留 hook）

默认 **`demo_seed_enabled=true`**，`demo_seed_value=20260906`。固定 seed，保证：

1. 玩家前两手有机会掺假  
2. 第 3～5 手内 AI 高概率质疑一次  
3. **约 90 秒内必现一次完整质疑**（拍1 指控 → 拍2 对峙/押注 → 拍3 翻牌结算）  

便于答辩不翻车。手感参数：`hesitate_dwell_ms=800`；SOFT/SLAM 见 `soft_max_velocity` / `slam_min_velocity`（px/s）。

---

## 8. 经济与商业化

**MVP 不做。** 无抽卡、无赌资、无付费加成功率。

---

## 9. 开放调参项（试玩会）

| 优先级 | 参数 | 备注 |
|--------|------|------|
| 锁定 | `lives_default` `hand_size_default` `max_play_cards` `min_play_cards` | 改前需制作人 |
| 锁定 | `challenge_only_seconds`（默认 8） | 手牌耗尽窗口 |
| 锁定 | `face_bet_enabled` / `face_bet_wrong` | 默认开 / 0 |
| 高（可调） | `turn_seconds` | 手感 |
| 高（可调） | Joker 张数 | 试玩 |
| 中 | AI `base_p_challenge` | |
| 低 | 面子分加分值 | |

---

## 10. 版本与审核

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-07-21 | 初值表，待试玩 |
| v0.2 | 2026-09-06 | 新增 `challenge_only_seconds: 8`；锁定基础键与押错 0 |
| v0.2.1 | 2026-09-06 | 补 `hesitate_dwell_ms` / SOFT·SLAM 速度阈值 / demo_seed 默认与 90s 质疑保证 |

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-06 | 补 challenge_only_seconds；与 GDD §10.1 对齐 | 升 v0.2 | 制作人 | 已消化 |
| 2026-09-06 | 手感参数与演示种子默认值补钉 | 升 v0.2.1 | 制作人 | 已消化 |
