# 大厅 P0 · 资产说明

| 项 | 内容 |
|----|------|
| 文档版本 | v0.1.1 |
| 状态 | 美术出图说明 · **不改玩法规则** |
| 读者 | 程序（DevEco 挂 `$r`）、UI 互审、负责人 |
| 色板 | [夜半酒馆-风格板.md](./夜半酒馆-风格板.md) token |
| 尺寸权威 | [关键资源规格清单.md](./关键资源规格清单.md) |
| 生成 | 仓库根 `scripts/gen_lobby_p0_art.py`（Pillow） |

> 本批把 **现挂同名 PNG** 从色块换成夜半酒馆插画。命名继续 **`art_*`**，**禁止 `lb_art_*`**。  
> 合入后大厅/对局 **不用改代码** 即可看见：`art_table_bg`、`art_dealer_bust_idle`（顺带 `art_dealer_bust_announce`）。  
> 新增皮（大厅独立底 / 主按钮 / 输入框）**未改 ets**，请 UI 跟一刀挂接。

---

## 1. 必须先覆盖（现挂同名 · 合入即可见）

UI #13 大厅已挂这些 `$r`。本批只换文件，不改 `lb_*` id。

| 文件名 | 尺寸(px) | 透明底 | 用途 | 现挂槽（已在 develop） |
|--------|----------|--------|------|------------------------|
| `art_table_bg.png` | 1080×2340 | 否 | 竖屏夜半酒馆：四角冷暗 `#0E1A22`、桌心酒红毡 `#2C1814`、顶烛、木饰边 | `lb_scr_lobby` 底 + `lb_scr_table` 底 · `$r('app.media.art_table_bg')` |
| `art_dealer_bust_idle.png` | 324×432 | 是 | 礼帽+面具荷官，暖烛侧光 | `lb_cmp_dealer`（Lobby）· `$r('app.media.art_dealer_bust_idle')` |
| `art_dealer_bust_announce.png` | 324×432 | 是 | 同角色播报：口型微张、举手示牌 | `lb_cmp_dealer`（Table）· `$r('app.media.art_dealer_bust_announce')` |

---

## 2. 新增皮（请 UI 跟一刀挂接）

本批 **只落文件**，不改 `Lobby.ets` / `LobbyPanel.ets`。

| 文件名 | 尺寸(px) | 透明底 | 建议 `$r` 挂点 | 建议控件 |
|--------|----------|--------|----------------|----------|
| `art_lobby_bg.png` | 1080×2340 | 否 | `$r('app.media.art_lobby_bg')` | `lb_scr_lobby` 底（若要比桌面更亮/带招牌；现用 `art_table_bg` 也可） |
| `art_btn_primary.png` | 720×144 | 是 | `$r('app.media.art_btn_primary')` | `lb_btn_quickstart` 常态（现为 `tavern_accent` Color） |
| `art_btn_primary_on.png` | 720×144 | 是 | `$r('app.media.art_btn_primary_on')` | `lb_btn_quickstart` pressed |
| `art_input_field.png` | 720×96 | 是 | `$r('app.media.art_input_field')` | `lb_cmp_nick_field`（现为 `tavern_panel` Color） |

未做独立 `art_lobby_sign.png`：没有单独木牌槽，招牌画进 `art_lobby_bg`。

---

## 3. 色板依据（只抄风格板）

| Token | Hex | 用在本批 |
|-------|-----|----------|
| `tavern_bg` | `#1A120E` | 底、衣帽暗部 |
| `tavern_felt` | `#2C1814` | 桌心毡 |
| `tavern_panel` | `#3D241C` | 木框、招牌、按钮常态底 |
| `tavern_candle` | `#E8B86D` | 径向烛光、焰芯 |
| `tavern_brass` | `#C4A46A` | 铜线、铆钉、帽带 |
| `tavern_paper` | `#F3E6C8` | 领/手套/招牌字 |
| `tavern_shadow` | `#0E1A22` | 四角冷暗、输入槽 |
| `tavern_accent` | `#F0C14B` | 按钮边 / 按下填充 |

制作：平涂 + 1 级暗面 + 径向烛光 + 木纹/铜线 + 轻噪点。bust / 按钮皮四角真透明。

脚手架 `scripts/gen_scaffold_assets.py` **跳过** 已插画的 `art_dealer_bust_*` 与 `art_table_bg`。

---

## 4. 自检

- [x] 现挂三张已换成插画，不是单色方块
- [x] 文件名 `art_*`，无 `lb_art_*`
- [x] 未改 UI #13 大厅 ets（不挡「合入即可见」）
- [x] 未改 GDD / PRD / 互动方案 / 数值 v0.2 正文

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-06 | 必须先覆盖现挂同名 PNG；新增皮请 UI 跟一刀挂接；不要只改文档 | 本批 v0.1.1：rebase UI #13，ets 不动 | 制作人/UI | 已改 |

---

*维护人：美术岗 · 2026-09-06*
