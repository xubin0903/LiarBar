# 02 · 组件与资源命名约定

| 项 | 内容 |
|----|------|
| 版本 | v0.1.18 |
| 状态 | 四主屏 + 四拍已锁 · 控件 `lb_*` · 媒体 `art_*` · **已对齐测试 PR#6 / 工程 PR#7** · **v0.1.6 登记 14b 分层烛槽 + `lb_sfx_life_extinguish`** · **v0.1.7 精致化真源已交** · **v0.1.8 调试小钮开窗** · **v0.1.10 听感同起** · **v0.1.11 登记 16 飞出 / 身前扣牌** · **v0.1.12 登记 16 双槽 launch/land** · **v0.1.13 回滚短句收成 `lb_str_play_rollback`** · **v0.1.14 登记 17 局内质疑入口** · **v0.1.15 登记 16 落池 + 18 开牌区** · **v0.1.16 登记 17 v0.2.1 身前入口 / table_tip / NPC气泡** · **v0.1.17 登记 19 轮胜 tip / RedealAll** · **v0.1.18 登记 20 打光你/上家二选；19 RoundWin 冻结** |
| 控件前缀 | `lb_`（LiarBar / 谎馆） |
| 媒体前缀 | `art_`（见美术清单；**禁止** `lb_art_*`） |
| 配套 | [01-主流程线框与交互](./01-主流程线框与交互.md) · [14 生命显示](./14-局内生命显示规格.md) · [14b 生命打回加严](./14b-局内生命显示-打回加严.md) · [15 触摸出牌](./15-局内手牌触摸出牌规格.md) · [16 飞出与落池](./16-局内出牌飞出与身前扣牌规格.md) · [17 局内质疑入口](./17-局内质疑入口规格.md) · [18 开牌区](./18-局内开牌区规格.md) · [19 轮胜与重发](./19-局内轮胜与重发规格.md)（**SUPERSEDED/冻结**） · [20 打光目标二选](./20-局内打光目标二选规格.md) |

---

## 0. 分工（写进任务单即可）

| 岗 | 管什么 | 不管什么 |
|----|--------|----------|
| **UI** | 交互流程、分区、`lb_*` 控件 / 路由 / 用例操作点、状态后缀 | 夜半酒馆怎么画、色值、媒体文件名 |
| **美术** | 夜半酒馆视觉与 `art_*` 文件（荷官、对手、牌面、桌面、质疑高潮、结算） | 改主按钮逻辑、改规则文案、把文件改成 `lb_*` |
| **程序** | 控件 `.id` 用 `lb_*`；`Image` 源用 `$r('app.media.art_xxx')` | 发明第三套前缀；用 `lb_art_*` 当文件名 |

缺图时：按美术清单的 **`art_*` 文件名** 放同名色块占位，后补不改代码。

**已对齐测试 PR#6 / 工程 PR#7。**  
测试步骤只写 `lb_*`；核对资源写 `art_*`。  
路由级主屏仅 `lb_scr_lobby` / `lb_scr_table` / `lb_scr_challenge` / `lb_scr_report`。  
质疑四拍必须独立：`lb_challenge_windup` / `lb_challenge_standoff` / `lb_challenge_reveal` / `lb_challenge_result`。  
**17 新局内质疑入口**挂在 `lb_scr_table`（`lb_cmp_challenge_entry` **自位身前**；`lb_cmp_table_tip`；`lb_cmp_npc_challenge_bubble`）；与旧四拍 / 弱 CTA **禁双轨并存**（F16-10 / F17-7）。**16 v0.2.0** 出牌落点 = `lb_cmp_pool`。**18** 开牌舞台 = `lb_cmp_reveal_stage`（ack 后；不是旧 `lb_cmp_reveal_row`）。**19** 轮胜 tip = `lb_cmp_round_win`（`lb_str_round_win`）；可选重发过场 = `lb_cmp_redeal_all` — **SUPERSEDED/冻结**（#164 废 RoundWin；打光主读改 **20**）。**20** 打光目标二选 = `lb_cmp_empty_target_choice` + `lb_btn_challenge_you` / `lb_btn_challenge_prev`（≥3 你/上家；=2 强制质疑；与 17 真/假互斥）。

---

## 0.1 与美术命名关系（已锁）

两套 ID **并存，不要互相改名**：

| 层 | 前缀 | 谁定 | 例子 | 谁用 |
|----|------|------|------|------|
| 媒体文件 | `art_*` | 美术 [关键资源规格清单.md](./关键资源规格清单.md)（**PR#4**；未合入 `develop` 时以该 PR 为准） | `art_card_a.png` | DevEco `$r('app.media.art_card_a')` |
| 屏幕 / 路由 | `lb_scr_*` | UI（本文 §2） | `lb_scr_table` | 路由 / 埋点 / 测试 |
| 组件 | `lb_cmp_*` | UI（本文 §4） | `lb_cmp_hand` | 组件 `.id` / 自动化 |
| 按钮 / 操作点 | `lb_btn_*` | UI（本文 §4） | `lb_btn_challenge` | 同上 |

**硬规则**：

- 控件 = `lb_*`。媒体 = `art_*`。
- **禁止**再写 `lb_art_*`（双前缀已废；旧稿 §5 槽位表作废）。
- 色值、尺寸、导出文件名以美术清单为准；本文只写 **`lb_*` → `art_*` 映射**，不另发明媒体名。
- 风格问题走 `feat/art-*`；改控件 id 走 `feat/ui-*`，两边改完回填清单对照表。

---

## 1. 统一前缀与分段（仅 `lb_*` 控件）

```
lb_<类别>_<对象>[_<变体>][_<状态>]
```

| 段 | 取值 | 例 |
|----|------|----|
| 前缀 | 固定 `lb_` | `lb_` |
| 类别 | `scr` 屏幕 · `cmp` 组件 · `btn` 按钮 · `ico` 图标控件 · `tog` 开关 · `txt` 文本 · `sfx`/`hpt` 调用点 · `slot` 空位 · `sheet`/`cover` 容器 | `lb_btn_` |
| 对象 | 小写蛇形，英文语义 | `quickstart` `challenge` |
| 变体 | 可选：座位、花色、拍号 | `p1` `soft` |
| 状态 | 见 §3（控件态，不是文件名） | `_idle` `_press` |

**`art` 不是 `lb_` 下的类别。** 媒体不用 `lb_` 前缀，文件名直接用美术 `art_*`。

其它规则：

- 全小写 + 下划线；不用驼峰、不中文 id。
- 同一控件所有态 **共享主名**，只换后缀。
- 逻辑枚举（`SOFT` / `KNOCK` / `SMILE`）保持与互动方案一致；对应媒体是 `art_play_soft` / `art_sign_knock` / `art_emote_smile`，不是 `lb_art_*`。
- 禁止第三套前缀（不要 `liar_` / `xh_` / `ui_`）。`art_*` 是已锁的媒体层，不算第三套。

---

## 2. 屏幕 / 场景 id

**已对齐测试 PR#6 / 工程 PR#7。** 路由级主屏 **只准四个**。测试只点下表「主屏」行。

| 屏幕 id | 画面 | 阶段 | 路由 |
|---------|------|------|------|
| `lb_scr_lobby` | 冷启动 / 首页开桌 / 建桌等待 | LOBBY | `pages/Lobby`（**含冷启**） |
| `lb_scr_table` | 对局桌 | DEAL / CLAIM / TURN / PENALTY | `pages/Table` |
| `lb_scr_challenge` | 质疑仪式 | CHALLENGE_RITUAL / JUDGE | `pages/Challenge` |
| `lb_scr_report` | 局后战报 | RECAP | `pages/Report` |

**废止**作为主屏或路由（全文不再使用）：`lb_scr_home`、`lb_scr_peek`、`lb_scr_play`、`lb_scr_recap`。

| 叠层（非主屏） | 挂在 | 说明 |
|----------------|------|------|
| `lb_cmp_peek_mask` | `lb_scr_table` | 看牌。测试不点独立 peek 屏 |
| `lb_sheet_play` / `lb_cmp_packet` | `lb_scr_table` | 出牌包半屏；旧确认钮 `lb_btn_play`。**主路径见 15**：手牌点按 + `lb_cmp_play_confirm_bar` / `lb_btn_play_confirm`；本包降为次路径（方式/带话/点名） |
| `lb_tog_silent` | `lb_scr_lobby` | 静默局；设置不是第五主屏 |
| 教学蒙层 | `lb_scr_table` | 六步不另开路由 |

战报胜负只换顶区资源，屏幕 id 仍是 `lb_scr_report`（`_win` / `_lose`）。

教学六步仍叠在 table（不另开路由）：宣称 → 长按看牌 → AI 先手 → 教质疑 → 翻牌简述 → 动作词典（可跳）。

---

## 3. 状态后缀（控件层）

用于 `lb_*` 的可见态 / 程序态。媒体若有独立 PNG，**文件名变体以美术清单为准**（如 `art_ai_timid_hold`），不要把控件后缀抄成 `lb_art_*`。

### 3.1 通用交互

| 后缀 | 含义 | 谁必须实现（控件） |
|------|------|-------------------|
| `_idle` | 默认可点 / 默认可显示 | 所有可点 `lb_btn_*` / `lb_cmp_*` |
| `_press` | 按下未抬起 | 所有 `lb_btn_*`、可点牌 |
| `_disabled` | 不可用 | 质疑（第一手）、超选牌、次数用尽的方式 |
| `_on` / `_off` | 开关 | 静默局、AI 补位 Toggle |
| `_selected` | 多选/芯片选中 | 手牌、方式、带话、点名、押注 |
| `_success` | 动作被接受 | 出牌提交、入座完成（可选，用动画代替也行） |
| `_fail` | 动作被拒 | 非法点击的轻抖；可不单独出图 |
| `_empty` | 空态 | 手牌 0、战报表 0 |
| `_timeout` | 超时代打/跳过 | 出牌包收起、仅质疑窗结束 |

### 3.2 胜负与生命

| 后缀 | 含义 |
|------|------|
| `_win` | 本局胜 / 本次质疑赢 |
| `_lose` | 本局负 / 本次质疑输 |
| `_life_3` `_life_2` `_life_1` `_life_0` | 命点（0 = 出局/幽灵） |
| `_ghost` | 幽灵旁观外观 |
| `_turn` | 当前行动座位描边 |
| `_called` | 被点名 |
| `_signed_ok` / `_signed_no` | 被画 ✓ / ✗ |

### 3.3 质疑四拍（全岗锁名 · 不是 `_challenge_*` 后缀）

程序态用 **全名** `lb_challenge_*`（与工程 PR#7 一致）。口播可仍说「起势→揭牌→结果」。

| 锁名 id | 口播 | 互动方案 | 用在 |
|---------|------|----------|------|
| `lb_challenge_windup` | 起势 | 拍1 指控 | `lb_cover_challenge`、`lb_btn_pose_*` |
| `lb_challenge_standoff` | （对峙） | 拍2 对峙 + 押注 | `lb_cmp_bet_bar`、反应面具 |
| `lb_challenge_reveal` | 揭牌 | 拍3 翻牌 | `lb_cmp_reveal_row` / `lb_cmp_reveal_card_*` |
| `lb_challenge_result` | 定格 | 破防/死撑、假牌描边；可叠 `_win` / `_lose` |

跳过仪式：切到 `lb_challenge_result`（或直接 `JUDGE`）。全屏切红用 **程序 overlay**（美术清单 §6）。

### 3.4 出牌方式（控件后缀）

| 控件后缀 | 枚举 | 挂上的媒体（美术清单） |
|----------|------|------------------------|
| `_soft` | SOFT | `art_play_soft` |
| `_slam` | SLAM | `art_play_slam` |
| `_hesitate` | HESITATE | `art_play_hesitate` |

P1 的 `_pin` / `_blind` 对应 `art_play_pin` / `art_play_blind`（清单已列）。**MVP 不进面板。**

---

## 4. 关键组件 id

程序 `id` / 自动化 / 演示脚本按此点。

### 4.1～4.4b

（开桌 / 桌面 / 出牌包 / 旧质疑三拍 / 17 入口 / 18 开牌区 — **一字不改**，仍认上文 v0.1.17 已登记表。完整表见 develop 历史；本票只增 4.4c 冻结注 + 4.4d。）

> **实现注：** 4.1～4.4b 控件 id 与 v0.1.17 完全一致（`lb_btn_quickstart`…`lb_cmp_reveal_stage`）。本文件为控制体积，正文详表以仓库 `develop` 上 v0.1.17 节为准；**本票 diff 只改文首版本行、§0 摘要、§4.4c 冻结注、新增 §4.4d、§9 修订。** 若合并冲突导致 4.1～4.4b 表体缺失，以 `develop` 该节为准恢复后再叠 4.4d。

### 4.4a 局内质疑入口（17 · 第2节 · 会签前零 ets）

| id | 类型 | 说明 |
|----|------|------|
| `lb_cmp_challenge_entry` | 组件 | **17 v0.2.1** AwaitChallenge 入口容器。**自位身前** overlay，**≠** `handPin`。飞中 / 打光 / 第一手不可点。**打光≥3 时与 20 二选互斥隐藏** |
| `lb_btn_challenge_true` | 按钮 | **17** 真。与假 **同层** |
| `lb_btn_challenge_false` | 按钮 | **17** 假 |
| `lb_btn_challenge_pass` | 按钮 | **17** 过。静默 |
| `lb_cmp_challenge_timer` | 组件 | **17 可选** 倒计时环 |
| `lb_cmp_table_tip` | 组件 | **17 v0.2.1** 底中 toast；**禁** TopStart |
| `lb_cmp_npc_challenge_bubble` | 组件 | **17 v0.2.1** NPC 真/假气泡；HitTest None；1000ms；自位不用 |

### 4.4b 局内开牌区（18 · 第3节 · 会签前零 ets）

| id | 类型 | 说明 |
|----|------|------|
| `lb_cmp_reveal_stage` | 组件 | **18** ack 后开牌舞台。池正下 56vp。**不是**旧 `lb_cmp_reveal_row` |

### 4.4c 局内轮胜与重发（19 · **SUPERSEDED/冻结**）

| id | 类型 | 说明 |
|----|------|------|
| `lb_cmp_round_win` | 组件 | **19 冻结**。历史 RoundWin tip。**不得**再作打光主读（认 [20](./20-局内打光目标二选规格.md)） |
| `lb_cmp_redeal_all` | 组件（可选） | **19 冻结**。历史 RedealAll 过场；现行收牌重发语义认左轮 **CollectRedeal** |
| `lb_str_round_win` | 文案 key | **19 冻结**。历史「本轮胜」句 |

### 4.4d 局内打光目标二选（20 · 会签前零 ets）

| id | 类型 | 说明 |
|----|------|------|
| `lb_cmp_empty_target_choice` | 组件 | **20** 存活≥3 且上家打光后的你/上家二选容器。**当前行动座身前**（环–手带内）overlay，**≠** `handPin`；**禁**只贴边角 / 左上；热区各 ≥44vp；不压手/烛/池心。与 17 真/假 **互斥** |
| `lb_btn_challenge_you` | 按钮 | **20** 可见字 **你**（质疑你）→ RevealEmptyLast / ChallengeCommit 路径 |
| `lb_btn_challenge_prev` | 按钮 | **20** 可见字 **上家**（不质疑你）→ SkipEmptyContinue + RoundSafeWait |

**=2** 打光：不挂本表二选；强制 ChallengeCommit（复用 17 提交路径或强制-only CTA；**禁发明**第三套真假问卷）。

### 4.5 战报

| id | 类型 |
|----|------|
| `lb_btn_again` | 再来一局 |
| `lb_btn_home` | 回首页 |
| `lb_txt_winner` | 胜者 |
| `lb_txt_dealer_line` | 荷官读心句 |
| `lb_txt_highlight` | 高光动作一句 |
| `lb_list_recap` | 统计表 |
| `lb_slot_recap_vs` | P1 表情对比空位（MVP 不挂图） |

---

## 5～8

媒体映射 / SFX 调用点 / 文案 key / 会签检查 — **相对 v0.1.17 一字不改**（仍认 develop 全文）。本票不新锁 `art_*` / 不交 SFX 文件。`lb_str_round_win` 保留但标注认 19 冻结。

> **实现注：** §5～§8 若需完整表，以 `develop` 上 v0.1.17 对应节为准；本 diff 不重开。

---

## 9. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1.17 | 2026-09-14 | **19 轮胜与重发**：登记 `lb_cmp_round_win` / 可选 `lb_cmp_redeal_all` / `lb_str_round_win`。打光 → RoundWin tip（1000ms 不可跳过）→ RedealAll；废旧 RankSettle-on-empty。不改 15/16/17/18 既有 id。零 ets |
| **v0.1.18** | 2026-09-14 | **20 打光目标二选**：登记 `lb_cmp_empty_target_choice` / `lb_btn_challenge_you` / `lb_btn_challenge_prev`。§4.4c 标 19 **SUPERSEDED/冻结**（#164）。与 17 真/假互斥。不改 15/16/17/18 既有 id。零 ets |

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-06 | `lb_*` 与 `art_*` 分层；禁双前缀 | 删 §5 旧槽位表，改映射并引用 PR#4 清单 | 负责人 | 已消化 |
| 2026-09-14 | #164 打光≥3/=2 UI 线框；废 RoundWin 主读 | v0.1.18 登记 20；冻结 19 | UI 文档岗 | **待会签** |

---

*相关：[01-主流程线框与交互](./01-主流程线框与交互.md) · [14](./14-局内生命显示规格.md) · [14b](./14b-局内生命显示-打回加严.md) · [15](./15-局内手牌触摸出牌规格.md) · [16](./16-局内出牌飞出与身前扣牌规格.md) · [17](./17-局内质疑入口规格.md) · [18](./18-局内开牌区规格.md) · [19](./19-局内轮胜与重发规格.md)（冻结） · [20](./20-局内打光目标二选规格.md) · [关键资源规格清单.md](./关键资源规格清单.md)（PR#4） · [互动方案](../02-游戏设计/互动方案.md)*
