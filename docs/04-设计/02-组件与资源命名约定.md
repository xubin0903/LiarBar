# 02 · 组件与资源命名约定

| 项 | 内容 |
|----|------|
| 版本 | v0.1.8 |
| 状态 | 四主屏 + 四拍已锁 · 控件 `lb_*` · 媒体 `art_*` · **已对齐测试 PR#6 / 工程 PR#7** · **v0.1.6 登记 14b 分层烛槽 + `lb_sfx_life_extinguish`** · **v0.1.7 精致化真源已交** · **v0.1.8 调试小钮开窗** |
| 控件前缀 | `lb_`（LiarBar / 谎馆） |
| 媒体前缀 | `art_`（见美术清单；**禁止** `lb_art_*`） |
| 配套 | [01-主流程线框与交互](./01-主流程线框与交互.md) · [14 生命显示](./14-局内生命显示规格.md) · [14b 生命打回加严](./14b-局内生命显示-打回加严.md) · [15 触摸出牌](./15-局内手牌触摸出牌规格.md) |

---

## 0. 分工（写进任务单即可）

| 岗 | 管什么 | 不管什么 |
|----|--------|----------|
| **UI** | 交互流程、分区、`lb_*` 控件 / 路由 / 用例操作点、状态后缀 | 夜半酒馆怎么画、色值、媒体文件名 |
| **美术** | 夜半酒馆视觉与 `art_*` 文件（荷官、对手、牌面、桌面、质疑高潮、结算） | 改主按钮逻辑、改规则文案、把文件改成 `lb_*` |
| **程序** | 控件 `.id` 用 `lb_*`；`Image` 源用 `$r('app.media.art_xxx')` | 发明第三套前缀；用 `lb_art_*` 当文件名 |

缺图时：按美术清单的 **`art_*` 文件名** 放同名色块占位，后补不改代码。

**已对齐测试 PR#6 / 工程 PR#7。**  
测试步骤只写 `lb_*`；核对资源写 `art_*`。  
路由级主屏仅 `lb_scr_lobby` / `lb_scr_table` / `lb_scr_challenge` / `lb_scr_report`。  
质疑四拍必须独立：`lb_challenge_windup` / `lb_challenge_standoff` / `lb_challenge_reveal` / `lb_challenge_result`。

---

## 0.1 与美术命名关系（已锁）

两套 ID **并存，不要互相改名**：

| 层 | 前缀 | 谁定 | 例子 | 谁用 |
|----|------|------|------|------|
| 媒体文件 | `art_*` | 美术 [关键资源规格清单.md](./关键资源规格清单.md)（**PR#4**；未合入 `develop` 时以该 PR 为准） | `art_card_a.png` | DevEco `$r('app.media.art_card_a')` |
| 屏幕 / 路由 | `lb_scr_*` | UI（本文 §2） | `lb_scr_table` | 路由 / 埋点 / 测试 |
| 组件 | `lb_cmp_*` | UI（本文 §4） | `lb_cmp_hand` | 组件 `.id` / 自动化 |
| 按钮 / 操作点 | `lb_btn_*` | UI（本文 §4） | `lb_btn_challenge` | 同上 |

**硬规则**：

- 控件 = `lb_*`。媒体 = `art_*`。
- **禁止**再写 `lb_art_*`（双前缀已废；旧稿 §5 槽位表作废）。
- 色值、尺寸、导出文件名以美术清单为准；本文只写 **`lb_*` → `art_*` 映射**，不另发明媒体名。
- 风格问题走 `feat/art-*`；改控件 id 走 `feat/ui-*`，两边改完回填清单对照表。

---

## 1. 统一前缀与分段（仅 `lb_*` 控件）

```
lb_<类别>_<对象>[_<变体>][_<状态>]
```

| 段 | 取值 | 例 |
|----|------|----|
| 前缀 | 固定 `lb_` | `lb_` |
| 类别 | `scr` 屏幕 · `cmp` 组件 · `btn` 按钮 · `ico` 图标控件 · `tog` 开关 · `txt` 文本 · `sfx`/`hpt` 调用点 · `slot` 空位 · `sheet`/`cover` 容器 | `lb_btn_` |
| 对象 | 小写蛇形，英文语义 | `quickstart` `challenge` |
| 变体 | 可选：座位、花色、拍号 | `p1` `soft` |
| 状态 | 见 §3（控件态，不是文件名） | `_idle` `_press` |

**`art` 不是 `lb_` 下的类别。** 媒体不用 `lb_` 前缀，文件名直接用美术 `art_*`。

其它规则：

- 全小写 + 下划线；不用驼峰、不中文 id。
- 同一控件所有态 **共享主名**，只换后缀。
- 逻辑枚举（`SOFT` / `KNOCK` / `SMILE`）保持与互动方案一致；对应媒体是 `art_play_soft` / `art_sign_knock` / `art_emote_smile`，不是 `lb_art_*`。
- 禁止第三套前缀（不要 `liar_` / `xh_` / `ui_`）。`art_*` 是已锁的媒体层，不算第三套。

---

## 2. 屏幕 / 场景 id

**已对齐测试 PR#6 / 工程 PR#7。** 路由级主屏 **只准四个**。测试只点下表「主屏」行。

| 屏幕 id | 画面 | 阶段 | 路由 |
|---------|------|------|------|
| `lb_scr_lobby` | 冷启动 / 首页开桌 / 建桌等待 | LOBBY | `pages/Lobby`（**含冷启**） |
| `lb_scr_table` | 对局桌 | DEAL / CLAIM / TURN / PENALTY | `pages/Table` |
| `lb_scr_challenge` | 质疑仪式 | CHALLENGE_RITUAL / JUDGE | `pages/Challenge` |
| `lb_scr_report` | 局后战报 | RECAP | `pages/Report` |

**废止**作为主屏或路由（全文不再使用）：`lb_scr_home`、`lb_scr_peek`、`lb_scr_play`、`lb_scr_recap`。

| 叠层（非主屏） | 挂在 | 说明 |
|----------------|------|------|
| `lb_cmp_peek_mask` | `lb_scr_table` | 看牌。测试不点独立 peek 屏 |
| `lb_sheet_play` / `lb_cmp_packet` | `lb_scr_table` | 出牌包半屏；旧确认钮 `lb_btn_play`。**主路径见 15**：手牌点按 + `lb_cmp_play_confirm_bar` / `lb_btn_play_confirm`；本包降为次路径（方式/带话/点名） |
| `lb_tog_silent` | `lb_scr_lobby` | 静默局；设置不是第五主屏 |
| 教学蒙层 | `lb_scr_table` | 六步不另开路由 |

战报胜负只换顶区资源，屏幕 id 仍是 `lb_scr_report`（`_win` / `_lose`）。

教学六步仍叠在 table（不另开路由）：宣称 → 长按看牌 → AI 先手 → 教质疑 → 翻牌简述 → 动作词典（可跳）。

---

## 3. 状态后缀（控件层）

用于 `lb_*` 的可见态 / 程序态。媒体若有独立 PNG，**文件名变体以美术清单为准**（如 `art_ai_timid_hold`），不要把控件后缀抄成 `lb_art_*`。

### 3.1 通用交互

| 后缀 | 含义 | 谁必须实现（控件） |
|------|------|-------------------|
| `_idle` | 默认可点 / 默认可显示 | 所有可点 `lb_btn_*` / `lb_cmp_*` |
| `_press` | 按下未抬起 | 所有 `lb_btn_*`、可点牌 |
| `_disabled` | 不可用 | 质疑（第一手）、超选牌、次数用尽的方式 |
| `_on` / `_off` | 开关 | 静默局、AI 补位 Toggle |
| `_selected` | 多选/芯片选中 | 手牌、方式、带话、点名、押注 |
| `_success` | 动作被接受 | 出牌提交、入座完成（可选，用动画代替也行） |
| `_fail` | 动作被拒 | 非法点击的轻抖；可不单独出图 |
| `_empty` | 空态 | 手牌 0、战报表 0 |
| `_timeout` | 超时代打/跳过 | 出牌包收起、仅质疑窗结束 |

### 3.2 胜负与生命

| 后缀 | 含义 |
|------|------|
| `_win` | 本局胜 / 本次质疑赢 |
| `_lose` | 本局负 / 本次质疑输 |
| `_life_3` `_life_2` `_life_1` `_life_0` | 命点（0 = 出局/幽灵） |
| `_ghost` | 幽灵旁观外观 |
| `_turn` | 当前行动座位描边 |
| `_called` | 被点名 |
| `_signed_ok` / `_signed_no` | 被画 ✓ / ✗ |

### 3.3 质疑四拍（全岗锁名 · 不是 `_challenge_*` 后缀）

程序态用 **全名** `lb_challenge_*`（与工程 PR#7 一致）。口播可仍说「起势→揭牌→结果」。

| 锁名 id | 口播 | 互动方案 | 用在 |
|---------|------|----------|------|
| `lb_challenge_windup` | 起势 | 拍1 指控 | `lb_cover_challenge`、`lb_btn_pose_*` |
| `lb_challenge_standoff` | （对峙） | 拍2 对峙 + 押注 | `lb_cmp_bet_bar`、反应面具 |
| `lb_challenge_reveal` | 揭牌 | 拍3 翻牌 | `lb_cmp_reveal_row` / `lb_cmp_reveal_card_*` |
| `lb_challenge_result` | 结果 | 定格 | 破防/死撑、假牌描边；可叠 `_win` / `_lose` |

跳过仪式：切到 `lb_challenge_result`（或直接 `JUDGE`）。全屏切红用 **程序 overlay**（美术清单 §6）。

### 3.4 出牌方式（控件后缀）

| 控件后缀 | 枚举 | 挂上的媒体（美术清单） |
|----------|------|------------------------|
| `_soft` | SOFT | `art_play_soft` |
| `_slam` | SLAM | `art_play_slam` |
| `_hesitate` | HESITATE | `art_play_hesitate` |

P1 的 `_pin` / `_blind` 对应 `art_play_pin` / `art_play_blind`（清单已列）。**MVP 不进面板。**

---

## 4. 关键组件 id

程序 `id` / 自动化 / 演示脚本按此点。

### 4.1 开桌

| id | 类型 | 画面 |
|----|------|------|
| `lb_btn_quickstart` | 主按钮 | home |
| `lb_btn_tutorial` | 次按钮 | home |
| `lb_btn_custom_table` | 次按钮「改桌规」 | home |
| `lb_btn_records` | 战绩 | home |
| `lb_btn_settings` | 设置 | home |
| `lb_btn_start` | 开打 | lobby |
| `lb_btn_skip_seat` | 跳过入座 | lobby |
| `lb_cmp_seat_self` | 自己座位 | lobby/table |
| `lb_cmp_seat_p1` `lb_cmp_seat_p2` `lb_cmp_seat_p3` | AI 座位（顺时针） | lobby/table |
| `lb_cmp_nick_field` | 昵称（智能填充） | lobby |
| `lb_tog_ai` | AI 补位 | lobby |
| `lb_tog_silent` | 静默局 | lobby/settings |
| `lb_cmp_playercount` | 人数芯片组 | lobby |

### 4.2 桌面 / 看牌

| id | 类型 | 说明 |
|----|------|------|
| `lb_cmp_topbar` | 顶栏 | 宣称+倒计时+荷官 |
| `lb_txt_claim` | 文本 | 当前宣称 |
| `lb_txt_timer` | 文本 | 剩余秒 |
| `lb_cmp_dealer` | 荷官位 | 桌面中央偏上 |
| `lb_cmp_pool` | 中央本手牌池 | 牌背+张数 |
| `lb_cmp_hand` | 手牌条 | 横向 List |
| `lb_cmp_card_{0-4}` | 单张手牌 | 下标 0 起；**15** 点按翻面+多选 |
| `lb_cmp_life_self` | 自己生命组 | **14 / 14b** · selfDock 近旁；**只** 3 枚烛 pip；禁止当面数字 |
| `lb_cmp_life_seat_p1` `p2` `p3` | 对手生命组 | **14** · 各座框近旁 |
| `lb_cmp_life_pip_{0-2}` | 单枚生命符号 | **14** 子实例；下标 0=最左/最满侧 |
| `lb_txt_life_self` | **废主路径**（14b） | 禁止当面辅读「3/2/1」；禁止 `showDigit` 当面完成。无障碍用组名+整数，不挂可见 Text |
| `lb_btn_debug_open` | 调试入口小钮 | **仅 debug/dev**（14b R-LIFE-5）。打开 `lb_cmp_debug_life`；正式包默认隐藏 |
| `lb_cmp_debug_life` | 调试窗 | **仅 debug/dev**。小窗 / 面板，不是第五主屏；关着时 −1 不露面 |
| `lb_btn_debug_life_dec` | 调试扣血 | **仅 debug/dev**。**只挂** `lb_cmp_debug_life` 内；自位/指定座 −1；正式包默认隐藏 |
| `lb_cmp_peek_mask` | 防窥遮罩 | peek |
| `lb_cmp_emote_wheel` | 表情轮盘 | peek |
| `lb_btn_emote_smile` 等 | 表情键 | 枚举小写 |
| `lb_btn_fake_surprise` / `lb_btn_fake_despair` | 假动作 | peek |
| `lb_btn_challenge` | 质疑 | table 底栏 |
| `lb_btn_hurry` | 催 | <5s 才挂树 |
| `lb_cmp_table_hit` | 桌面热区 | 双击/三连 KNOCK |
| `lb_btn_sign_ok` / `lb_btn_sign_no` | ✓✗ | 头像弹出 |
| `lb_privacy_on` | 调用点 | 揭牌瞬间防窥（非可见控件） |

### 4.3 出牌包

| id | 类型 |
|----|------|
| `lb_sheet_play` / `lb_cmp_packet` | 半屏容器（测试点 packet）。**15 后为次路径**（方式/带话/点名） |
| `lb_btn_play_cancel` | 取消 |
| `lb_btn_play` | 旧出牌确认（锁名不废；**不再是唯一主路径**） |
| `lb_cmp_play_confirm_bar` | **15** 桌上确认条（合法选中才出） |
| `lb_btn_play_confirm` | **15** 新主提交（脚手架已预列） |
| `lb_btn_style_soft` / `_slam` / `_hesitate` | 方式芯片 |
| `lb_btn_line_skip` | 不带话（默认选中） |
| `lb_btn_line_honest` | 「整整齐齐」 |
| `lb_btn_line_taunt` | 「你敢开？」 |
| `lb_cmp_callout` | 点名头像行 |
| `lb_txt_play_count` | 「已选 n/3」 |

### 4.4 质疑三拍

| id | 类型 |
|----|------|
| `lb_cover_challenge` | 全屏容器 |
| `lb_btn_challenge_skip` | 跳过 |
| `lb_cmp_challenger` / `lb_cmp_accused` | 左右角色 |
| `lb_btn_pose_slam` / `_toast` / `_point` | 拍桌 / 举杯 / 指人 |
| `lb_btn_react_hold` / `_break` / `_mock` | 死撑 / 破防 / 反嘲 |
| `lb_cmp_reveal_row` | 待翻扣牌 |
| `lb_cmp_reveal_card_{0-2}` | 单张扣牌（最多 3） |
| `lb_cmp_bet_bar` | 押注条（仅拍2） |
| `lb_btn_bet_true` / `lb_btn_bet_fake` | 全真 / 有假 |
| `lb_cmp_beat_dots` | 进度三点 |
| `lb_btn_smash_cup` | 砸杯 |
| `lb_txt_judge` | 结果句 |

### 4.5 战报

| id | 类型 |
|----|------|
| `lb_btn_again` | 再来一局 |
| `lb_btn_home` | 回首页 |
| `lb_txt_winner` | 胜者 |
| `lb_txt_dealer_line` | 荷官读心句 |
| `lb_txt_highlight` | 高光动作一句 |
| `lb_list_recap` | 统计表 |
| `lb_slot_recap_vs` | P1 表情对比空位（MVP 不挂图） |

---

## 5. 媒体文件名：直接用美术 `art_*`

本文 **不再维护** 独立切图表。文件名、尺寸、状态变体、P0 最低挂包清单一律以：

> [关键资源规格清单.md](./关键资源规格清单.md)（美术 **PR#4**；尚未合入 `develop` 时见该 PR / 同目录美术文档）

程序：`Image($r('app.media.art_xxx'))`。下面只列 **关键 `lb_*` → `art_*`**，方便绑资源；未列出的控件默认无独立插画（风格板色 + 文字）。

权威对照若与下表冲突：**以美术清单正文为准**（清单合入后请美术删掉其文中残留的 `lb_art_*` 别名列）。

### 5.1 荷官

| `lb_*` 控件 | 挂 `art_*` | 说明 |
|-------------|------------|------|
| `lb_cmp_dealer` | `art_dealer_bust_idle` / `art_dealer_bust_announce` | 常态 / 播报；P1 翻牌旁白用 `art_dealer_bust_reveal` |
| `lb_scr_lobby` 中区 | 同上 idle，可裁切 | 不出第二份 bust |
| `lb_scr_report` 荷官句旁 | `art_dealer_bust_announce`（P1 `art_dealer_bust_settle`） | |

### 5.2 对手 / 自己

| `lb_*` 控件 | 挂 `art_*` | 说明 |
|-------------|------------|------|
| `lb_cmp_seat_self` | `art_frame_player_idle` | 脸用系统头/字；`_turn` 可用 `art_frame_player_turn`（P1）或程序描边 |
| `lb_cmp_seat_self` `_ghost` | `art_frame_player_ghost` | 出局 |
| `lb_cmp_seat_self` `_called` | 叠加 `art_fx_candle_stare` | 或用 `art_frame_player_named` |
| `lb_cmp_seat_p1` `p2` `p3` | `art_ai_timid_*` / `art_ai_shark_*` / `art_ai_karen_*` | 按人格：`_idle` / `_hold` / `_break` |
| 命点（座位内实例） | `art_life_cup_full` / `art_life_cup_empty` | 控件态 `_on` / `_off` |
| `lb_cmp_life_self` / `lb_cmp_life_seat_p1` `p2` `p3` | **终验 / 主绑：** `art_life_candle_body` + `art_life_candle_flame_full` / `_hurt` / `_dying` + `art_life_candle_extinguish_0` `_1` `_2` `_3` | 可选 `flame_full_0…n`。旧整图 `art_life_candle_full` / `_hurt` / `_dying` **暂留兜底**。柱须＞焰。权威 [14b §2.2](./14b-局内生命显示-打回加严.md)。真件 [生命烛精致化-资产交件.md](./生命烛精致化-资产交件.md) |
| `lb_cmp_emote_wheel` / `lb_btn_emote_*` | `art_emote_smile` `smirk` `frown` `brow_up` `avert` `stare_nod` `panic` | 枚举对齐互动方案 §4.1 |
| `lb_btn_sign_ok` / `lb_btn_sign_no` | `art_sign_ok` / `art_sign_no` | |
| `lb_cmp_table_hit` | `art_sign_knock`；波纹可用 `art_fx_knock_ripple`（P1）或程序圈 | 三连 = 同一文件加强表现 |

### 5.3 牌面

| `lb_*` 控件 | 挂 `art_*` | 说明 |
|-------------|------------|------|
| `lb_cmp_card_{0-4}` 默认 | `art_card_back` | |
| 揭开 / 选中 | `art_card_a` / `k` / `q` / `joker` | 选中用组件描边，不必第二张图 |
| `lb_cmp_peek_mask` | `art_card_privacy_mask` | |
| `lb_cmp_pool` | `art_pool_slot` + `art_card_back` | 池中更小：同一 `art_card_back` 缩放 |
| `lb_cmp_reveal_card_{0-2}` | 背→面 + `art_card_glow` | 假牌定格用程序描边，清单未单列 fake_mark |

不要出完整扑克花色。Joker 不作宣称目标（无 `art_claim_badge_joker`）。

### 5.4 桌面

| `lb_*` 控件 / 屏 | 挂 `art_*` | 说明 |
|------------------|------------|------|
| `lb_scr_table` 底 | `art_table_bg` | 接法 A（P0）；切红用 overlay，不换底图 |
| `lb_scr_lobby` 底 | 复用 `art_table_bg` 或荷官 bust | 清单未单列 lobby 底 |
| `lb_txt_claim`（或 `lb_ico_claim_*`） | `art_claim_badge_a` / `k` / `q` | |
| `lb_btn_smash_cup` | `art_fx_smash_cup` | P1 敬酒可复用 |
| `lb_btn_play_confirm` | **`art_btn_play_confirm` / `art_btn_play_confirm_on`**（15 已交件） | 毡木 + 铜 / 烛金边。**禁止**系统灰钮当终稿。同名覆盖 #103 占位 |

静默局 **不换桌面图**，只关震动和音效。

### 5.5 质疑高潮

| `lb_*` 控件 | 挂 `art_*` | 说明 |
|-------------|------------|------|
| `lb_cover_challenge` | **不另出全屏 JPG** | 风格板 overlay（清单 §6） |
| `lb_btn_pose_slam` / `_toast` / `_point` | `art_challenge_pose_slam` / `_toast` / `_point` | |
| `lb_btn_react_hold` / `_break` | `art_ai_*_hold` / `_break` | 按被质疑者人格 |
| `lb_btn_react_mock` | hold + `art_emote_smirk` | 清单：反嘲不强制第三张脸 |
| `lb_btn_challenge_skip` | `art_challenge_skip_hint`（P1） | 可先纯文字 |
| `lb_btn_bet_true` / `_fake` | 程序文字钮 | 清单：不强制插画 |
| 惩罚泼酒 | `art_fx_wine_splash` | 接在三拍后的 PENALTY |
| 出局 | `art_frame_player_ghost` 或对手图去饱和 | 清单未单列 stamp，不另发明文件 |

### 5.6 结算

| `lb_*` 控件 | 挂 `art_*` | 说明 |
|-------------|------------|------|
| `lb_scr_report` 顶区 | `art_result_bar_win` / `art_result_bar_lose` | 控件态 `_win` / `_lose` |
| `lb_txt_highlight` | `art_hl_long_bluff` / `art_hl_best_challenge` / `art_hl_worst_break` | 按高光类型选一 |
| `lb_slot_recap_vs` | — | P1，清单未列，本批不挂图 |

---

## 6. 音效 / 震动占位名（调用点，不是媒体文件）

`lb_sfx_*` / `lb_hpt_*` 是 **程序调用 id**，不是 PNG。音频文件若后补，由美术/音频另定文件名，**不要**写成 `lb_art_*`。

| id | 触发 | 静默局 |
|----|------|--------|
| `lb_sfx_seat` | 入座 | 可留 |
| `lb_sfx_peek` | 揭牌（长按 peek） | 可留 |
| `lb_sfx_card_flip` | **15** 点按翻面；**14b** 掉命起势复用本槽 | 可留；真源 `sfx_card_flip.wav`（精致化交件）。生命线 **不**另造 `lb_sfx_life_drop` |
| `lb_sfx_card_select` | **15** 选中抬起 | 可留；**本线不交文件** |
| `lb_sfx_play_confirm` | **15** 点确认出牌 | 可留；**本线不交文件** |
| `lb_sfx_life_extinguish` | **14b** 被扣那一枚灭尽 | 可留；真源 `sfx_life_extinguish.wav`；**不**另起平行槽名 |
| `lb_sfx_soft` / `_slam` / `_hesitate` | 出牌方式 | slam 关 |
| `lb_sfx_knock` / `_knock_heavy` | 敲桌 | 可降到 0 |
| `lb_sfx_hurry` | 催 / <5s | **关** |
| `lb_sfx_callout` | 点名 | 可留 |
| `lb_sfx_challenge_windup` / `lb_sfx_challenge_standoff` / `_flip` / `_flip_fake` | 对应四拍 windup/standoff/reveal | 嘲讽向 **关** |
| `lb_sfx_judge_win` / `_lose` | 结果 | 嘲讽向 **关** |
| `lb_sfx_toast_slam` | 砸杯 | **关** |
| `lb_sfx_recap` | 战报入场 | 可留 |
| `lb_hpt_press` | 通用短震 | **关强震动** |
| `lb_hpt_slam` | 甩出 | **关** |
| `lb_hpt_judge` | 扣命 | **关** |

---

## 7. 文案 key（避免硬编码中文散落）

程序用 key，中文写在字符串表。此处只列主路径，便于和线框对上。

| key | 默认中文 |
|-----|----------|
| `lb_str_quickstart` | 快速开桌 |
| `lb_str_start` | 开打 |
| `lb_str_play` | 出牌 |
| `lb_str_play_illegal_count` | 张数不符 |
| `lb_str_play_not_your_turn` | 非己回合 |
| `lb_str_play_over_select` | 超选 |
| `lb_str_challenge` | 质疑 |
| `lb_str_challenge_disabled_first` | 第一手还没人出过牌 |
| `lb_str_skip` | 跳过 |
| `lb_str_skip_line` | 不带话 |
| `lb_str_again` | 再来一局 |
| `lb_str_peek_hint` | 按住看牌 · 松手收起 |
| `lb_str_bet_true` | 全真 |
| `lb_str_bet_fake` | 有假 |
| `lb_str_hand_empty` | 没有牌了，只能质疑 |
| `lb_str_claim_fmt` | 本轮出 {rank} |

口语别称「骗子酒馆」不进按钮。正式名：**谎馆**。

---

## 8. 会签检查（UI ↔ 美术）

- [ ] 本文无任何 `lb_art_*`
- [ ] 控件 `.id` 全是 `lb_scr_*` / `lb_cmp_*` / `lb_btn_*` 等
- [ ] `Image` 源只引用美术清单里的 `art_*`，不在本表发明新媒体名
- [ ] 质疑全屏底走 overlay，不要求三套全屏切图
- [ ] 牌面只有 A/K/Q/Joker + 背
- [ ] 表情是酒馆符号，不是写实人脸
- [ ] 未自作主张加商店、充值、联网房间入口
- [ ] 测试脚本继续点 `lb_*`，不改去点文件名

---

## 9. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-09-06 | 首稿（含现已废止的 `lb_art_*` 槽位表） |
| v0.1.1 | 2026-09-06 | 会签美术命名：禁 `lb_art_*`；媒体跟 `art_*`；补 §0.1 |
| v0.1.2 | 2026-09-06 | 四主屏 + 四拍锁名；对齐测试 PR#6 / 工程 PR#7 |
| v0.1.3 | 2026-09-11 | 登记 14/15：`lb_cmp_life_self` / `lb_cmp_life_seat_pN` / `lb_cmp_play_confirm_bar` / `lb_btn_play_confirm`；SFX 槽 `lb_sfx_card_flip` / `_select` / `play_confirm`（不交文件）；`lb_sheet_play` 降为次路径 |
| v0.1.4 | 2026-09-11 | 回填美术交件：生命主读 `art_life_candle_*`；确认铬 `art_btn_play_confirm*`（#103 同名换绑） |
| v0.1.5 | 2026-09-12 | 14b：废 `lb_txt_life_self` 主路径；登记 `lb_btn_debug_life_dec`；SFX 调用点（后由 v0.1.6 改复用 `lb_sfx_card_flip`） |
| v0.1.6 | 2026-09-12 | 14b PM+Art：登记 `art_life_candle_body` / `flame_*` / `extinguish_*`；SFX 复用 `lb_sfx_card_flip` + `lb_sfx_life_extinguish`；废 `lb_sfx_life_drop` |
| v0.1.7 | 2026-09-12 | 精致化真源：`sfx_card_flip` / `sfx_life_extinguish` 已交；主绑 `flame_{full,hurt,dying}` + `extinguish_0…3`；可选 `flame_full_0…n`；旧三态暂留 |
| v0.1.8 | 2026-09-12 | 14b v0.1.2：登记 `lb_btn_debug_open` + `lb_cmp_debug_life`；`lb_btn_debug_life_dec` 改挂窗内 |

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-06 | `lb_*` 与 `art_*` 分层；禁双前缀 | 删 §5 旧槽位表，改映射并引用 PR#4 清单 | 负责人 | 已消化 |

---

*相关：[01-主流程线框与交互](./01-主流程线框与交互.md) · [14-局内生命显示规格](./14-局内生命显示规格.md) · [14b-局内生命显示-打回加严](./14b-局内生命显示-打回加严.md) · [15-局内手牌触摸出牌规格](./15-局内手牌触摸出牌规格.md) · [关键资源规格清单.md](./关键资源规格清单.md)（PR#4） · [互动方案](../02-游戏设计/互动方案.md)*
