# 14 · 局内生命显示规格（线 A）

| 项 | 内容 |
|----|------|
| 版本 | v0.1.1 |
| 状态 | **方案会签草案** · Rebuild 加严认 [14b](./14b-局内生命显示-打回加严.md) · 未会签禁止落 ets |
| 范围 | `lb_scr_table` 生命 **表现层**：线框槽、信息层级、三态表、`lb_*` id、`art_*` **占位槽**、掉命轻反馈时序窗、无障碍、失败短句 |
| 读者 | UI、美术、策划、测试、鸿蒙、**@LiarBar负责人** |
| 对照 | [GDD §2.2 生命](../02-游戏设计/GDD.md) · [01 §5 对局桌](./01-主流程线框与交互.md) · [02 命名](./02-组件与资源命名约定.md) · [11 横屏线框](./11-局内横屏与自适应布局.md) · [14a 语义](./14a-生命显示-语义与文案.md) · [14b 打回加严](./14b-局内生命显示-打回加严.md) · [夜半酒馆-风格板](./夜半酒馆-风格板.md) · [关键资源规格清单](./关键资源规格清单.md) |
| 本 PR | **只交文档**。零 ets、零 rawfile、零 media |

> **UI 管**：线框、尺寸建议、状态表、控件 id、可测失败短句。  
> **策划管**：GDD / 状态机散文、生命语义措辞（本线不改规则正文）。  
> **美术管**：夜半酒馆皮与 `art_*` 终稿；本文只挂 **美术候选** 槽，**不点终稿题材**。  
> 命名：控件 `lb_*`；媒体 `art_*`。**禁止** `lb_art_*`。

---

## 0. 边界（写死）

### 0.1 写什么 / 不写什么

| 层 | 本文做什么 | 本文不做什么 |
|----|------------|--------------|
| 规则 | 只 **引用** GDD：每人 3 命；0 命 → 幽灵旁观；剩 1 人有命 → 胜 | **不改** 命数、扣命条件、胜负判定；不发明回血 / 护盾 / 第 4 命 |
| 质疑 | 点名「质疑仍冻」 | **不扩** 四拍、不写质疑光感新闸 |
| 贴底 | 生命条 **不得** 挡 `lb_cmp_hand` | **不重开** 11 flush 闸 / `padB` / `HAND_RING_GAP`；**贴底硬修本轮搁置** |
| 出牌 | 只声明不挡手牌 / 荷官 / 池 | 出牌触摸主路径见 [15](./15-局内手牌触摸出牌规格.md) |
| 落地 | 会签后才开 `feat/client-*` | **本 PR 零 ets** |

**硬闸三句（会签必须看见）：**

1. **不改数值规则（仍 3 命）**。  
2. **质疑仍冻**。  
3. **贴底硬修本轮搁置**（不绑 11 手牌贴底硬修；生命槽自己找空位，不改 flush / `padB`）。

### 0.2 现挂缺口（对照，不改码）

现 Rebuild 自位已挂烛，但 `lb_cmp_life_self` 旁仍见辅读「3」，且单枚焰可压过烛身——**加严见 [14b](./14b-局内生命显示-打回加严.md)**。  
本线目标：酒馆皮生命 **只靠烛火主读**。自位当面 **禁止** 旁挂「3/2/1」（卸 `lb_txt_life_self` 主路径 / 禁止 `showDigit` 当面完成）。与 14b 冲突时 **以 14b 为准**。

---

## 1. 目的

把「3 / 2 / 1 / 0」换成 **一眼能感到还剩几口气** 的酒馆生命显示。

- 主读：烛火符号组（`art_life_candle_full` / `_hurt` / `_dying`）。杯 / 筹 / 牌记不再当主读交差。  
- 当面 **不挂** 阿拉伯数字。无障碍可读剩余整数，不渲染 `lb_txt_life_self`。  
- 三态必须可辨：**满血 / 残血 / 濒死**。可选第四态 **幽灵/出局**。  
- 自己与对手 **层级不同**；横屏小档仍须可读。

生命 **语义**（何为扣命、何为出局、何为胜）仍认 GDD §2.2。策划并行会签措辞；UI **不另写胜负规则**。

---

## 2. 信息层级

### 2.1 自己 vs 对手

| | 自己 `lb_cmp_life_self` | 对手 `lb_cmp_life_seat_pN` |
|--|------------------------|---------------------------|
| 位置 | 底带 / `selfDock` 近旁；**下手身份条**，不进手牌行 | 各座框近旁（头像下沿或内侧），不进桌心 |
| 体量 | **主生命**：符号更大、间距更松 | **次生命**：同构缩小，三态仍可指 |
| 数字 | **禁止**当面旁挂「3/2/1」（14b R-LIFE-1） | 不挂可见数字 |
| 动效 | 掉命反馈可略强（仍轻、短） | 同构更弱，避免抢宣称 / 荷官 |
| 禁区 | **不得**压 `lb_cmp_hand` 最左牌、不得进 hand 水平中线 | **不得**压 bust / 池 / 宣称 |

一眼验收：评委能指「这是我的命、那是他的命」，不靠读座位名才能分清。

### 2.2 三态（+ 可选幽灵）

规则仍是 3 命整数。UI 把剩余命 **映射** 成表现态。策划可改口播用词；映射表本线锁定给测试。

| 表现态 | 剩余命（GDD） | 主读意图 | 当面数字 |
|--------|---------------|----------|----------|
| **满血** | 3 | 三枚符号都「还在」；暖、稳；**柱＞焰** + 轻循环 | **不挂** |
| **残血** | 2 | 一枚已灭/空/裂；还能撑；柱仍在、焰更矮 | **不挂** |
| **濒死** | 1 | 只剩一枚在喘；残芯+烟循环；不写成「已死」 | **不挂** |
| **幽灵/出局**（可选） | 0 | 符号全灭或去饱和；叠座位 `_ghost` | **不挂** |

禁止只用红/绿换色交差三态。必须靠 **枚数 + 形变**（灭烛 / 空杯 / 裂筹 / 牌记翻面等，题材由美术会签）。

0 命行为仍认 GDD：出局 = 幽灵旁观，不可出牌 / 质疑。本线只画外观，**不扩旁观 UX**。

### 2.3 横屏小档可读

对照 11 L-small：环座已缩小。生命不得先被卸掉。

| 档 | 自己符号建议 | 对手符号建议 | 数字 |
|----|--------------|--------------|------|
| 常档横屏 | 单枚 **≥ 20vp**；三枚总宽约 72～96vp | 单枚 **≥ 14vp** | 当面不挂 |
| L-small | 自己单枚 **≥ 16vp**；对手 **≥ 12vp** | 可收成贴座一列 | 当面不挂；不得用数字补可读 |

失败：小档只剩看不清的细线点、或生命被卸成空。

---

## 3. 线框槽位（`lb_scr_table`）

横屏桌。生命是 **环上饰件**，不是底栏通栏。不改 11 手牌贴底数字。

```
┌─ 顶栏宣称 / 倒计时 ──────────────────────────────────────┐
│                          [p2 老千]                         │
│                         life_p2                            │
│                                                            │
│  [p3 杠精]          荷官 bust + 池          [p1 怂货]      │
│  life_p3            （禁挡）                 life_p1        │
│                                                            │
│  selfDock + life_self          ← 底带左槽，不压最左牌      │
│            ┌──── lb_cmp_hand ────┐                         │
│            │ 背 背 背 背 背      │  ← 生命不得伸进此带     │
└────────────┴─────────────────────┴─────────────────────────┘
```

| 槽 | 控件 | 锚 | 不得挡 |
|----|------|----|--------|
| 自己 | `lb_cmp_life_self` | `selfDock` / `lb_cmp_seat_self` **近旁**（头下或名旁）；底带左槽内 | `lb_cmp_hand`、最左 `lb_cmp_card_0`、确认条（见 15） |
| p1 | `lb_cmp_life_seat_p1` | `lb_cmp_seat_p1` 框下沿或内侧 | 荷官、池、顶栏 |
| p2 | `lb_cmp_life_seat_p2` | `lb_cmp_seat_p2` 框下沿（顶栏下，座仍上沿中） | 宣称、bust |
| p3 | `lb_cmp_life_seat_p3` | `lb_cmp_seat_p3` 框下沿或内侧 | 荷官、池、顶栏 |

3 人桌：缺座不挂生命。不把三座收成一排。

**叠层建议（只建议，零 ets）：** 生命跟座位同组，不进 `lb_cmp_hand` 的 `layoutWeight` 链，不进桌心 `tableHeart`。

**贴底硬修搁置：** 本线 **不** 用生命条去「垫高 / 沉底」手牌，**不** 改 `HAND_RING_GAP` / `padB`。确认条近底安全区上沿的可点口径只认 [15](./15-局内手牌触摸出牌规格.md)，**不**回绑 11 flush 闸。

---

## 4. 组件 id 与美术占位槽

### 4.1 控件（本线锁名）

| id | 类型 | 挂在 | 说明 |
|----|------|------|------|
| `lb_cmp_life_self` | 组件 | `lb_scr_table` · selfDock 近旁 | 自己生命组（**只** 3 枚烛 pip；禁止当面数字） |
| `lb_cmp_life_seat_p1` | 组件 | `lb_cmp_seat_p1` 近旁 | 下家 |
| `lb_cmp_life_seat_p2` | 组件 | `lb_cmp_seat_p2` 近旁 | 对家 |
| `lb_cmp_life_seat_p3` | 组件 | `lb_cmp_seat_p3` 近旁 | 上家 |
| `lb_cmp_life_pip_{0-2}` | 可选子实例 | 上列组内 | 单枚符号，下标 0=最左/最满侧；测试可点子或只点组 |
| `lb_txt_life_self` | **废主路径** | 勿再挂可见树 | 禁止当面辅读「3/2/1」；禁止 `showDigit` 当面完成（14b R-LIFE-1） |
| `lb_btn_debug_life_dec` | 调试钮 | 仅 debug/dev | 自位/指定座 −1；正式包默认隐藏（14b R-LIFE-5） |

座位锁名不改：`lb_cmp_seat_self` / `_p1` / `_p2` / `_p3`。生命是 **旁挂组件**，不是新座位。

状态后缀沿用 [02 §3.2](./02-组件与资源命名约定.md)：`_life_3` `_life_2` `_life_1` `_life_0`、`_ghost`。  
表现态别名（文档/测试口播）：`_full` = 满血、`_hurt` = 残血、`_dying` = 濒死。程序仍以剩余命整数为准。

### 4.2 `art_*` 占位槽（美术候选 · UI 不点终稿）

清单 **已有** 可先占位（未会签题材前可同名色块）：

| 已列槽 | 来源 | 说明 |
|--------|------|------|
| `art_life_cup_full` / `art_life_cup_empty` | [清单 §2.5](./关键资源规格清单.md) | 酒杯满/空 |
| `art_life_chip_full` / `art_life_chip_empty` | 清单「筹码替代」 | **与酒杯二选一**；文档与工程只留一套 |

本线 **另列题材候选**（会签后至多留 **一套** 主题；未选中的名字 **不要**当终稿文件）：

| 候选主题 | 占位槽（未锁） | 三态怎么表现 |
|----------|----------------|--------------|
| 烛（**已锁主读**） | `art_life_candle_full` / `_hurt` / `_dying` | 旺火 / 半烬 / 将灭。**柱＞焰**；循环锁 **同槽帧动画**（14b）。静图占位 ≠ 终验 |
| 筹 | 优先复用已列 `art_life_chip_*` | 三枚叠减；濒死只留一枚裂纹 |
| 杯 | 优先复用已列 `art_life_cup_*` | 三杯；空杯 = 已失命 |
| 牌记 | `art_life_mark_full` / `_hurt` / `_dying` | 三点 / 裂记 / 翻扣 |

**UI 不点终稿。** 美术会签后回填 [02](./02-组件与资源命名约定.md) 映射与清单。  
缺图：同名色块占位，**后补不改** `lb_*` id。  
禁止再造 `lb_art_*`。禁止本线烘焙新 PNG。

### 4.3 当面数字（14b 覆盖）

| 要 | 不要 |
|----|------|
| 自位只靠 3 枚烛认命 | 旁挂「3/2/1」或 `showDigit` 当面完成 |
| 色盲仍能靠枚数 + 形变认态 | 只靠红绿区分满/残/濒 |
| 无障碍读组名 + 剩余整数 | 关掉符号只留数字当完成 |

---

## 5. 状态表与掉命反馈

### 5.1 稳态

| 态 | 自己 | 对手 | 可点？ |
|----|------|------|--------|
| 满血 | 三枚「在」；暖、稳 | 同构缩小 | 否（生命不是按钮） |
| 残血 | 两枚在、一枚灭/空 | 同上 | 否 |
| 濒死 | 一枚在；残芯+烟循环，不抢宣称 | 同上，闪更弱 | 否 |
| 幽灵/出局 | 全灭或去饱和；座位 `_ghost` | 同上 | 否；不挡旁观（旁观 UX 本线不扩） |

回血 / 加命：**本线不做**。不写治愈动画。

### 5.2 掉命轻反馈（只写意图 + 时序窗）

触发：`PENALTY` 已按 GDD 扣 1 命之后。本线 **不改** 扣谁、扣几命。

| 意图（题材会签后择一或组合） | 时序窗 | 说明 |
|------------------------------|--------|------|
| 摇 / 灭（烛/焰） | **180～320ms**（14b 覆盖旧窗）；可微调，**禁 0ms** | 被扣那一枚；**禁止**瞬间换图硬切 |
| 裂（杯/筹/牌记） | 若仍用备选题：**150～240ms** | 不要整桌抖；主读已锁烛 |

| 要 | 不要 |
|----|------|
| 只动被扣那一枚 + 所属座位 | 全屏闪红（质疑仍冻，不借惩罚做质疑皮） |
| 静默局：画面在、强震关 | 强震当唯一反馈 |
| 结束后落到新稳态（残血或濒死或幽灵） | 反馈后仍显示旧命数 |
| 灭/摇可看见过渡 | **掉命硬切换态**（14b R-LIFE-4） |

音效：调用点可挂 `lb_sfx_life_drop` / `lb_sfx_life_extinguish`（[02](./02-组件与资源命名约定.md) 已登记）；**本线不新造音频文件**。无文件不挡视觉闸。

---

## 6. 无障碍

| 项 | 建议 |
|----|------|
| 色盲 | 三态靠 **枚数 + 形变**；禁止只靠红/绿 |
| 对比 | 符号相对桌布对比建议 **≥ 3:1**；当面不挂数字，不必再给辅读字配色 |
| 最小尺寸 | 见 §2.3；热区不是按钮，但评委须能 **指认**，不能小于建议 vp |
| 屏幕朗读（可选） | 组名「自己生命 / 怂货生命」+ 剩余整数；不读题材昵称当规则 |
| 静默局 | 不关生命画面 |

---

## 7. 失败短句（测试可抄）

合入 ≠ 终验。未会签勿按短句改 ets。

| # | 短句 | 不过 |
|---|------|------|
| F14-1 | **生命仍是纯数字** | 主读仍是裸 `3/2/1`，无符号组 |
| F14-2 | **三态不可辨** | 满血/残血/濒死靠色盲或凑近才分清；或 2 与 1 同形 |
| F14-3 | **挡手牌或荷官** | 生命压 `lb_cmp_hand` / 最左牌 / `lb_cmp_dealer` / `lb_cmp_pool` |
| F14-4 | **自己与对手层级混淆** | 四座生命同大、分不清谁是自己 |
| F14-5 | **小档生命不可读** | L-small 卸生命或缩成无法指认的点 |
| F14-6 | **数字当唯一完成** | 卸符号只留数字交差 |
| F14-7 | **改了命数规则** | 出现 ≠3 的默认命、回血、第 4 格当规则 |
| F14-8 | **绑了贴底硬修** | 为生命改 `padB` / `HAND_RING_GAP` / 重开 11 flush 闸 |

Rebuild 五闸 **抄 [14b](./14b-局内生命显示-打回加严.md)**（不另起炉灶）：

| # | 短句（14b 原句） | 不过 |
|---|------------------|------|
| R-LIFE-1 | **自位生命旁仍有数字** / **生命仍靠数字辅读** | 自位旁挂「3/2/1」或 `showDigit` 当面完成 |
| R-LIFE-2 | **火苗比烛身高** / **焰压烛** | 单枚焰高于/压过蜡柱 |
| R-LIFE-3 | **焰静止死图当完成** | 满/残/濒无循环意图；静图当终验 |
| R-LIFE-4 | **掉命硬切换态** | 瞬间换图；时序 0ms |
| R-LIFE-5 | **无调试扣血无法验熄灭**（仅对调试包） | 调试包无 `lb_btn_debug_life_dec` |

质疑入口被生命挡住、或生命票顺带做质疑四拍 = 一并打回（质疑仍冻）。

---

## 8. 会签

- [ ] @LiarBar负责人 规格终审  
- [ ] @LiarBar策划 生命语义措辞仍认 GDD §2.2；本线无新胜负规则  
- [ ] @LiarBar美术 主读已锁烛；柱＞焰 + 同槽循环认 [14b](./14b-局内生命显示-打回加严.md)  
- [ ] @LiarBar UI 槽位不挡手牌 / 荷官 / 池；自己≠对手层级；当面禁数字  
- [ ] @LiarBar测试 失败短句可抄（含 14b R-LIFE-1～5）；合入 ≠ 终验  

**未会签禁止落 ets。** 合入本 PR ≠ 局内生命终验。

---

## 9. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1.0 | 2026-09-11 | 会签草案：去裸数字；三态；self / pN 槽；美术候选槽；掉命轻反馈时序窗；贴底硬修搁置；质疑仍冻 |
| v0.1.1 | 2026-09-12 | Rebuild 补刀回写：当面禁数字；主读锁烛；灭/摇 180～320ms 禁硬切；失败短句指向 [14b](./14b-局内生命显示-打回加严.md)。冲突以 14b 为准 |

*维护人：UI 文档岗 · 2026-09-12 · docs only · 会签前零 ets*
