# 03 · 开场与大厅交互 v2

| 项 | 内容 |
|----|------|
| 版本 | v0.2.1 |
| 状态 | **UI 规格草案** · 色板 token hex **已会签** · **现挂图只是占位，不是 v2 交件** |
| 范围 | **打开 App + 大厅**（`lb_scr_lobby`）。**冻结局内**：不改 `lb_scr_table` / 引擎 / `demo_seed` / 规则 |
| 读者 | 程序、美术、测试、负责人 |
| 依赖 | [01-主流程线框](./01-主流程线框与交互.md) §4 开桌规则仍有效 · [02-命名约定](./02-组件与资源命名约定.md) · [夜半酒馆-风格板](./夜半酒馆-风格板.md) · [大厅P0-资产说明](./大厅P0-资产说明.md) · [PRD F-01 / F-11 / F-13](../01-产品/PRD.md) |
| 落地分支 | **禁止立刻开码。** 须 **美术真分层新资产** + **本文（UI）会签** 两齐，才开 **`feat/client-lobby-v2`** |

> **终验打回**：褐金单一、全静态、无加载/进场、像色块拼贴。  
> **硬约束**：现挂 `art_lobby_bg` / `art_dealer_bust_*` / `art_btn_primary*` **只能标「占位」**。用户打回的就是那套褐金平涂，**不能当 v2 交件**。  
> **本版目标**：冷启动有 1.5～2.5s 时间轴；大厅五分区；CTA 跳出；轻动效；静默局关音仍有字幕。画面交件等美术「真分层」。

---

## 0. 本文边界

**写什么**：冷启动分拍、大厅信息架构、微动效、已会签色板（用来验收分层，不是给平涂填色）、锁名、用户逐步验收、ArkUI 提示。

**不写什么 / 本批禁止改**：

| 冻结 | 口径 |
|------|------|
| 规则 v0.2 | 出牌权、质疑范围、手牌耗尽、命数、Joker 不作宣称 |
| 局内 | `lb_scr_table` / `lb_scr_challenge` / `lb_scr_report` 布局与仪式 |
| 引擎 | `MatchEngine` / `MatchDirector` / 阶段转移 |
| 种子 | `demo_seed` / `demo_seed_value=20260906` |
| 锁名 | §6 已锁 `lb_*` **不动**；媒体仍 `art_*`，**禁止 `lb_art_*`** |

[01 §4](./01-主流程线框与交互.md) 的人数 / 命数 / 快速开桌 / 入座规则 **继续有效**。本文只重做「打开 App + 大厅」的观感与分区。

### 0.1 开发落地条件（负责人硬约束）

同时满足才允许开 `feat/client-lobby-v2` 写 ets：

1. **美术真资产已交**：景深底、荷官层次、**独立启动静帧**、CTA 质感（见 §5.1）。不是把现挂平涂换个文件名。  
2. **本文（UI 说明）已会签**。  
3. 用 §5 **已会签 hex** 做 **分层验收**（远景 / 中景 / 近景 / CTA 能指认 token），不是「屏幕上有这些色就过」。

缺任一条件：**不开开发分支**。本 PR 只交文档。

---

## 1. 终验问题与目标（可对照）

| 打回点 | 现状（P0 占位） | v2 必须变成 |
|--------|-----------------|-------------|
| 褐金单一 | 现挂 `art_lobby_bg` / bust / `btn_primary` 是褐金平涂 | **真分层**新资产；现图 **不得**当交件 |
| 全静态 | 进页即稳态，无加载、无进场 | 冷启动 **1.5～2.5s（默认帧表 ~2.0s，可调）** |
| 像色块拼贴 | 标题 / bust / 褐卡 / 主按钮纵向堆叠 | 顶栏 / 荷官区 / 设置卡 / 主 CTA / 次级「进桌看看」 |
| 无招呼 | 入座满句「本桌四人，酒已满。」 | 进场默认：**「夜深了，位子给你留着。」**（策划已给） |

---

## 2. 冷启动时间轴（默认总长 ~2.0s，可调 1.5～2.5s）

同一路由 `lb_scr_lobby` 内播完，**不另开启动页路由**（废止 `lb_scr_home` 仍成立）。  
时长做成常量（建议 `BOOT_MS_*`），总长 clamp 到 **1500～2500ms**。缩短时按比例压各拍，**不得删掉**黑场、加载、荷官、stagger、稳态任一段。

### 2.1 开场帧表（默认 ~2.0s）

资源分两列：**占位** = 仓库里现挂的褐金平涂，**只供对拍，不是 v2 交件**；**v2 待美术** = 真分层新资产，会签前用建议 `art_*` 槽位占名。

| 拍 | 时刻 | 时长 | 画面（用户看见） | 占位（现挂，非交件） | v2 待美术（真分层） |
|----|------|------|------------------|----------------------|---------------------|
| T0 黑场 | 0 | 一帧～可忽略 | 全屏深底，无按钮可点 | 程序色 `tavern_bg` `#1A120E` | 同 token；不是独立路由 |
| T0→静帧淡入 | 0～0.35s | 350ms | 黑场淡入 **独立启动静帧**（招牌 / 门脸），无闪白 | `art_lobby_bg`（平涂底，**占位**） | **`art_splash_still` 必出**。禁止再写「复用 lobby_bg 即可交件」 |
| 加载暗示 | 0.35～0.9s | 550ms | 静帧已在；中央或底沿转圈 **或** 细进度条 | 程序 `Progress`；条色可先用 token | 细条 `tavern_candle`、轨道 `tavern_shadow`；可叠在真静帧上 |
| 荷官亮相 | 0.9～1.3s | 400ms | 胸像淡入；idle→announce **轻切**（交叉淡化，禁硬切闪白） | `art_dealer_bust_idle` / `_announce`（平涂 bust，**占位**） | **荷官层次**：侧光 / 面具 / 肩线可分层；槽位名可仍是这对 `art_*`，**必须换真图** |
| 分区 stagger + 招呼 | 1.3～1.7s | 400ms | 设置卡 → 主 CTA →「进桌看看」错开入场；字幕打出招呼 | 现 Lobby 褐卡 + 平涂 CTA（**占位**） | 景深底上叠窄卡；**CTA 质感**新皮 |
| 大厅底显现 | 随 0.9～1.7s | — | 静帧之下（或切过）露出大厅景深，不是一张糊满的褐块 | `art_lobby_bg`（**占位**） | **景深底**（远冷雾 / 中木纹或桌 / 近烛）。槽位可仍名 `art_lobby_bg`，**必须换真分层图** |
| 稳态 | 1.7～2.0s | 300ms | 轴结束；**可点** `lb_btn_quickstart`；呼吸或烛光仍轻动 | 现按钮皮（**占位**） | `art_btn_primary` / `_on` **换质感**，禁止褐金平涂交件 |

**可点规则**：轴未到稳态前，主 CTA **不可点**。总轴播完（默认 2.0s，允许落在 1.5～2.5s）必须可点。「跳过开场」= **P1**，本批不强制。

### 2.2 分区 stagger 顺序（默认 1.3～1.7s；缩轴时仍保持先后）

每层间隔约 80～100ms，单层 `opacity 0→1` + `translateY 12→0` + `scale 0.96→1`：

1. 顶栏  
2. 荷官区（胸像已在上一拍；本拍补字幕条）  
3. 设置卡（昵称 / 人数 / 静默）  
4. 主 CTA `lb_btn_quickstart`  
5. 次级「进桌看看」

### 2.3 荷官招呼字幕

| 项 | 口径 |
|----|------|
| 默认句（策划已给） | **夜深了，位子给你留着。** |
| 建议 key | `lb_str_dlr_lobby_greet`（新增文案 key，不改已锁控件 id） |
| 控件 | 挂在 `lb_cmp_dealer` 旁或下方 |
| 入座满句 | `lb_str_dlr_lobby_full`「本桌四人，酒已满。」**仍留给入座满** |
| 口播 | 非静默可播；无素材则静音，不挡时间轴 |
| **静默局** | **关口播 / 关强震**；**字幕必须仍出** |
| 停留 | 至少保持到稳态；若淡出，≥1.5s 后、不挡 CTA |

---

## 3. 大厅信息架构

竖屏。**减少「一张大褐卡片糊满」**：设置项收进 **窄卡**，荷官与 CTA **不进同一块褐底**。

```
┌─────────────────────────────┐  ← 顶栏（薄，透景深底）
│ 谎馆              [战绩]    │
├─────────────────────────────┤
│                             │
│     荷官胸像 lb_cmp_dealer  │  ← 荷官区（独立，不套褐卡）
│     「夜深了，位子给你留着。」│
│                             │
├─────────────────────────────┤  ← 设置卡（窄，留左右边距）
│  昵称  [ lb_cmp_nick_field ]│
│  人数 n · 命 3（只读）      │
│  静默局  [ lb_tog_silent ]  │
├─────────────────────────────┤
│                             │
│    【 快速开桌 】            │  ← 主 CTA，从背景跳出
│    lb_btn_quickstart        │
│                             │
│         进桌看看            │  ← 次级，弱
└─────────────────────────────┘
```

| 分区 | 放什么 | 视觉 | 锁名 |
|------|--------|------|------|
| 顶栏 | 标题「谎馆」；战绩可弱 | 透景深底，不要满宽褐条 | `lb_str_app_title`；战绩 `lb_btn_records`（可后挂） |
| 荷官区 | 胸像 + 招呼字幕 | 胸像叠在景深上；字幕 `tavern_paper` | `lb_cmp_dealer` |
| 设置卡 | 昵称、人数、静默；「一键填上次」可留弱链 | **窄卡**，左右 ≥16vp，圆角 12vp，底 `tavern_panel` | `lb_cmp_nick_field` · `lb_cmp_playercount` · `lb_tog_silent` |
| 主 CTA | 「快速开桌」 | **独立于设置卡**；质感皮 + 高对比 | `lb_btn_quickstart` |
| 次级 | 「进桌看看」 | 小于 CTA，色 `tavern_mute` | 文案 `lb_str_nav_peek`。热区若补 `.id` 建议 `lb_btn_peek_table` |

### 3.1 CTA 必须从背景跳出来

| 要 | 不要 |
|----|------|
| 填充 token `tavern_accent` `#F0C14B`，字 `tavern_bg` `#1A120E`；**交件必须是质感图** | 用现挂平涂 `art_btn_primary` 当 v2 完成 |
| 热区高 ≥56vp | 缩成细字链 |
| 按下 ≤80ms 缩放 | 无按压、与毡面糊成一块 |

命数只读 3。人数 / 昵称 / 静默逻辑不变（F-11 / F-13）。  
「进桌看看」升格为可见次级入口，**不得**做成第二个主 CTA。

---

## 4. 微动效（禁止全程死静）

| 动效 | 何时 | 量级 | ArkUI 提示 |
|------|------|------|------------|
| 静帧淡入 | 0～0.35s | `opacity 0→1`，350ms EaseOut | `animateTo({ duration: 350, curve: Curve.EaseOut })` |
| 加载转圈 / 细条 | 0.35～0.9s | 转圈或细条 0→100%；结束淡出 150ms | `Progress` Ring 或 Linear |
| 荷官交叉淡化 | 0.9～1.3s | idle↔announce `opacity` 交叉 400ms | 两张 `Image` 叠 `Stack` |
| 分区 stagger | 1.3～1.7s | 每层 80～100ms；`opacity` + `translateY(12)` + `scale(0.96→1)` | 各层独立 `animateTo` |
| **荷官呼吸** | 稳态循环 | `scale 1.00↔1.02`，周期 1.4～1.8s | 往返；不要位移头像 |
| **烛光闪** | 稳态循环 | 焰芯 `opacity 0.72↔1.0`，周期 0.5～0.9s | 极淡 `tavern_candle` 罩 |
| **CTA 按压缩放** | 用户按下 | **≤80ms** `scale 1→0.96`；抬起 100～120ms 回 1 | 对齐 01 §1.3 |
| 字幕打出 | stagger 拍 | 整句淡入 200ms | 静默局同样出字 |

呼吸与烛光至少做一种，鼓励都做但更轻。  
**禁止**进页全静态、闪白、大弹跳。静默局：**动效与字幕仍播**，只关口播与强震。

---

## 5. 色彩对接（token hex 已会签 · 用来分层验收）

> **色板 token 已会签**（与 [风格板 §2](./夜半酒馆-风格板.md) 一致）。  
> **会签的是 hex，不是现挂平涂图。** v2 用下表验收 **真分层**：远景偏 `tavern_shadow`、暖烛用 `tavern_candle`、面板 `tavern_panel`、CTA `tavern_accent` 跳出。  
> 禁止：把现挂褐金平涂涂上这些 hex 就算 v2 通过。微调 hex 须改风格板并再会签。

| Token | Hex（已锁） | 分层验收用在 | 对比要求 |
|-------|-------------|--------------|----------|
| `tavern_bg` | `#1A120E` | 深底、黑场、CTA 字 | 冷启动第一像素 |
| `tavern_candle` | `#E8B86D` | 暖烛强调、标题、加载条、近景焰 | 点缀，不当满屏底 |
| `tavern_shadow` | `#0E1A22` | 远景冷雾、四角、加载轨道 | 与暖烛对打 |
| `tavern_panel` | `#3D241C` | 设置卡 / 木纹中层 | **只给窄卡** |
| `tavern_felt` | `#2C1814` | 毡/木辅底 | 不当大厅唯一中区 |
| `tavern_brass` | `#C4A46A` | 铜饰描边 | 细线，可 40% 透明 |
| `tavern_accent` | `#F0C14B` | CTA 高对比填充 | 必须亮于 panel / felt |
| `tavern_bg`（字） | `#1A120E` | CTA 字 | 压在 accent 上 |
| `tavern_paper` | `#F3E6C8` | 主文字、招呼字幕 | 深底上一眼可读 |
| `tavern_mute` | `#8A735A` | 「进桌看看」、弱提示 | 不当主 CTA 字 |

程序用 `color.json` 同名 token，页面不散写 hex。

### 5.1 资产：占位 vs v2 交件（负责人硬约束）

现挂 **只能标占位**。用户打回的就是褐金平涂那套。**不能当 v2 交件。**

| 槽位 `art_*`（文件名可留） | 占位（现挂 · 非交件） | v2 待美术（真分层 · 交件） |
|---------------------------|----------------------|---------------------------|
| `art_lobby_bg` | 褐金平涂大厅底 | **景深底**：远冷雾 / 中木纹或桌 / 近烛。换图，不是改 token 名 |
| `art_dealer_bust_idle` | 平涂胸像 | **荷官层次**（侧光、面具、肩）常态 |
| `art_dealer_bust_announce` | 平涂播报 | 同上层次的播报态；与 idle 可交叉淡化 |
| `art_btn_primary` / `art_btn_primary_on` | 平涂主按钮 | **CTA 质感**（铜/烛光体积，按下态独立） |
| `art_input_field` | 平涂输入皮 | 可后补质感；不挡大厅分层验收 |
| **无现成交件** | 曾写「P0 不强制、复用 lobby_bg」——**作废** | **`art_splash_still` 独立启动静帧必出**，与景深底不是同一张糊图 |

媒体只认 `art_*`。**禁止** `lb_art_*`。新文件先登记再挂 `$r`。

v2 美术最低交件包：

1. 景深底（可仍名 `art_lobby_bg`，必须是新分层图）  
2. 荷官层次（idle + announce）  
3. 独立启动静帧 `art_splash_still`  
4. CTA 质感（primary + on）

---

## 6. 锁名（不动）

| id | 层 | 说明 |
|----|----|------|
| `lb_scr_lobby` | 主屏 | 冷启动 + 大厅，同一路由 |
| `lb_btn_quickstart` | 主 CTA | 轴结束后可点；逻辑仍是默认开桌 |
| `lb_cmp_nick_field` | 设置卡 | 智能填充 |
| `lb_cmp_dealer` | 荷官区 | 换真图 / 动效，不改 id |
| `lb_tog_silent` | 设置卡 | 静默局；不是第五主屏 |
| `lb_cmp_playercount` | 设置卡 | 人数 |

可新增：`lb_str_dlr_lobby_greet`；次级热区建议 `lb_btn_peek_table`。  
**禁止** `lb_art_*`。

---

## 7. 用户验收清单（严格 1.5–2.5s 逐步）

冷启动总长必须落在 **1.5～2.5s**（默认帧表 ~2.0s）。下表按时间走；**先看后点**。任一步失败即未通过。  
时刻按默认 2.0s 轴书写；若总长调到 1.5s / 2.5s，**拍顺序不变**，只按比例伸缩，验收仍勾每一拍。

| # | 时刻（默认轴） | 用户点哪里 | 必须看到什么 |
|---|----------------|------------|--------------|
| 1 | 0s | **打开 App**。手离开屏幕，不点任何按钮 | 黑场（`tavern_bg` `#1A120E`）。不是立刻一张褐卡表单，也不是现挂平涂大厅当完成态 |
| 2 | 0～0.35s | 仍不点 | 独立启动静帧从黑场 **淡入**（v2 交件是 `art_splash_still`；现 `art_lobby_bg` 只算占位预览）。无闪白 |
| 3 | 0.35～0.9s | 仍不点 | **加载暗示**：转圈或底沿细进度条。能感到「在进馆」 |
| 4 | 0.9～1.3s | 仍不点 | 荷官出现；idle→announce **轻切**。此拍点 `lb_btn_quickstart` **不应**开桌 |
| 5 | 1.3～1.7s | 仍不点 | 设置卡、主 CTA、「进桌看看」**错开**入场（能看出先后） |
| 6 | 与 #5 同时 | 看荷官旁字幕，不点 | 默认句：**「夜深了，位子给你留着。」** |
| 7 | 1.7～2.0s（总长不得早于 1.5s、不得晚于 2.5s） | 看整屏，仍先不点 | 五分区可辨：顶栏 / 荷官 / 窄设置卡 / 主 CTA / 「进桌看看」。设置卡 **不是**糊满中下区的大褐块。主按钮从景深底跳出（质感 CTA，不是平涂褐金块） |
| 8 | 轴结束后（≤2.5s） | **点 `lb_btn_quickstart`（「快速开桌」）** | 可点；按下整钮缩放（≤80ms）；进入默认开桌（规则不变）。若 2.5s 后仍在转圈或点了没反应 = 不通过 |
| 9 | 稳态停 3s | **不点**，只看 `lb_cmp_dealer` / 近景烛 | 呼吸 **或** 烛光轻闪。全程死静 = 不通过 |
| 10 | 回大厅后 | **点 `lb_tog_silent` 打开静默局**，再杀掉进程 **重新打开 App**（再走 1.5～2.5s） | 无口播、无强震；**招呼字幕仍在**：「夜深了，位子给你留着。」 |
| 11 | 稳态后 | **点次级「进桌看看」**（不要点成主 CTA） | 弱入口、不抢主按钮；能进桌预览。局内画面不在本规格验收范围 |
| 12 | 进桌后 | 看 Table / 引擎，**不要当本清单的美术验收** | 局内 / `demo_seed` **未改**。大厅 v2 **不得**用现挂平涂冒充已完成 |

**分层验收（美术交真图后加勾，hex 用 §5）**：能指认远景冷雾、中景木纹/桌、近景烛、荷官层次、独立静帧、CTA 质感。只有 token 色块拼贴 = 不通过。

---

## 8. ArkUI 提示（会签齐后才写进 `feat/client-lobby-v2`）

| 区域 | 建议 |
|------|------|
| 根 | `Stack`：黑场色 → 静帧 → 景深底 → 控件层 |
| 时间轴 | `@State bootPhase`；`aboutToAppear` 启动；总长 clamp 1500～2500 |
| 淡入 / stagger | `animateTo`：`opacity` / `scale` / `offset` |
| 荷官 | 两图交叉 `opacity`；稳态呼吸 `scale` |
| CTA | 透明 `Button` 盖质感皮；补按压 `scale` |
| 加载 | `Progress`，结束移除或 `visibility` |
| 字幕 | 静默局跳过 `lb_sfx_*`，`Text` 照写 |

**现在不要改** `Lobby.ets` / `LobbyPanel.ets`。条件见 §0.1。

---

## 9. 明确不做

| 不做 | 原因 |
|------|------|
| 改规则 / GDD / 数值 / PRD | v0.2 冻结 |
| 改 Table / Challenge / Report / 引擎 | 先重做打开 App + 大厅 |
| 改 `demo_seed` | 演示高潮路径不动 |
| 改 §6 已锁 `lb_*` | 测试会断 |
| 媒体写成 `lb_art_*` | 已废双前缀 |
| 本批 ets 大改 | 真资产 + 本文会签齐之前 **不开** `feat/client-lobby-v2` |
| 把现挂 `art_lobby_bg` / bust / `btn_primary` 当 v2 交件 | 负责人硬约束；用户已打回褐金平涂 |
| 用「复用 lobby_bg」代替独立静帧 | v2 **必出** `art_splash_still` |
| 3D / 联网房 / 商店 | 一页纸不做清单 |

---

## 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.2 | 2026-09-07 | 首稿：帧表、五分区、微动效、色板 hex |
| v0.2.1 | 2026-09-07 | 负责人硬约束：现挂图只标占位；帧表拆「占位 / v2 待美术」；验收按 1.5–2.5s 逐步点看；落地须真资产+本文会签齐 |

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-07 | 终验打回大厅品质；冻结局内 | 只写开场+大厅 v2 规格 | 负责人 | 已消化 |
| 2026-09-07 | 色板 token hex 锁定 | 色板表填实 | 美术 | **hex 已会签**（不是现图已会签） |
| 2026-09-07 | 现挂 bg/bust/btn 只能占位；要真分层；验收按 1.5–2.5s；齐套才开开发分支 | 改帧表两列、改清单、改落地条件 | 负责人 | **硬约束 · 已改文档** |

---

*相关：[04-设计 README](./README.md) · [01-主流程线框](./01-主流程线框与交互.md) · [风格板](./夜半酒馆-风格板.md) · [大厅P0-资产](./大厅P0-资产说明.md)*
