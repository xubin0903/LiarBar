# 14a · 生命显示 · 语义与文案（线 A）

| 项 | 内容 |
|----|------|
| 文档版本 | **v0.1.1**（配合 [#100](https://github.com/xubin0903/LiarBar/pull/100)） |
| 状态 | **策划语义 / 文案** · 合入 ≠ 终验 · 不改数值 |
| 主笔 | 策划线 A |
| 对照 | [14 局内生命显示规格](./14-局内生命显示规格.md)（**线框 / `lb_*` / 硬闸以 14 为准**）· [GDD §2.2 / §9.1](../02-游戏设计/GDD.md) · [数值 `lives_default=3`](../02-游戏设计/数值与牌堆配置.md) · [荷官裁定句](../03-鸿蒙与AI/AI荷官与牌友规格.md) |
| 本 PR | **只交 docs**。零 `ets` / `rawfile` / `media`。**不重写 14 线框像素** |

> **规则 v0.2 已锁。** 每人 **3 命** 一字不改。质疑仍冻。贴底硬修本轮搁置。  
> 与 #100 冲突时：**14 的 `lb_*` 与硬闸为准**。本文只拍板怎么说、怎么分层读。

---

## 0. 边界

| 写 | 不写 |
|----|------|
| 主隐喻、自己/对手语气、掉命局内短句 | 槽位 vp、ASCII 线框、掉命摇/灭时序（认 14 §3 / §5） |
| 引用 14 已锁 `lb_*` | 发明第三套生命 id；改 3 命 / 扣命 / 出局 |
| 语义向失败短句（对齐 F14-*） | 替美术点烛/杯/筹终稿 |

---

## 1. 主隐喻拍板：烛火

生命 = 桌上那簇火。满则旺，残则矮，濒死将灭，出局火灭。

| 剩余命（GDD，不改） | 14 表现态 | 本文口播档 | 挂在（**14 锁名**） |
|---------------------|-----------|------------|---------------------|
| 3 | 满血 `_full` / `_life_3` | **燃** | `lb_cmp_life_self` · `lb_cmp_life_seat_p1` `p2` `p3` |
| 2 | 残血 `_hurt` / `_life_2` | **弱** | 同上；单枚可用 `lb_cmp_life_pip_{0-2}` |
| 1 | 濒死 `_dying` / `_life_1` | **将灭** | 同上 |
| 0 | 幽灵 `_life_0` / `_ghost` | **灭** | 同上 + 座位 `_ghost` |

辅读数字挂 14 的 `lb_txt_life_self`（自己可选次读）。**不得**只靠数字交差——与 F14-1 / F14-6 同闸。枚数必须等于剩余命。

**杯 / 筹为一句备选：** 14 §4.2 已列杯、筹、牌记为美术候选；语义主锁烛火，本文不点终稿。现挂 `art_life_cup_*` / `art_life_chip_*` 可占位，措辞仍说「火 / 燃 / 弱 / 将灭」，不说 HP、生命值、血条。

---

## 2. 自己 vs 对手 · 语气

控件认 14：自己 = `lb_cmp_life_self`（近 `lb_cmp_seat_self`）；对手 = `lb_cmp_life_seat_pN`。

| | 自己 | 对手 |
|--|------|------|
| 层级 | **强提示** | **弱化**（14：同构缩小） |
| 语气 | 对你说：你的火、你还剩 | 陈述：那头、{昵称} 的火 |
| 掉命 | 可短字幕，贴自己生命组 | 座位闪一下，不占屏心、不大字喊命 |
| 将灭 | 「就剩芯了」可以更紧 | 仍弱，不恐吓 |

禁止层级颠倒（对齐 F14-4）：自己弱成看不清、对手大字「还剩 n 命」。  
禁止系统腔。面子分仍弱化，不当第二套命。

---

## 3. 掉命局内提示（样例）

**不改、不覆盖**已锁裁定句：

- `lb_str_dlr_judge_success`：有假。{被质疑者} 掉 1 命。  
- `lb_str_dlr_judge_fail`：全真。{质疑者} 掉 1 命。  
- `lb_str_dlr_penalty_life`：{昵称} 还剩 {命} 命。  
- `lb_str_dlr_penalty_out`：{昵称} 出局，旁观看戏。

氛围句在裁定之后或不挡裁定。静默局口播可关、字幕仍在。质疑仍冻：不写四拍新词。

| 对象 | 掉完后 | key 建议 | 样例（酒馆虚张声势，不油腻） |
|------|--------|----------|------------------------------|
| 自己 | 2 | `lb_str_life_self_2` | 你的火矮了一截。还剩 2。 |
| 自己 | 1 | `lb_str_life_self_1` | 就剩芯了。看住。 |
| 自己 | 0 | — | 走已锁出局句 |
| 对手 | 2 | `lb_str_life_other_2` | {昵称} 的火晃了晃。 |
| 对手 | 1 | `lb_str_life_other_1` | {昵称} 那头只剩芯。 |
| 对手 | 0 | — | 走已锁出局句 |

不要：「亲，生命值只剩一滴～」「加油哦」。不要惨叫长文。  
对手句不重复念数字（次读在座位上）。裁定句与氛围句禁止同时抢屏心。

常驻辅读：自己 `你 · {命}`（可叠 `lb_txt_life_self`）；对手只留枚数 / 极小焰。

---

## 4. 一句约束

生命组不得挡 `lb_cmp_hand` 与 `lb_cmp_dealer`（14 F14-3）。本文不借「生命要贴底」重开 11/12。不改 3 命。

---

## 5. 失败短句（对齐 F14-* + 语义向）

线框 / 可读闸 **抄 14 §7**。下表只补语义向，并与 F14 对上号。合入 ≠ 终验。

| ID | 短句 | 对齐 | 不过 |
|----|------|------|------|
| F14-1 | **生命仍是纯数字** | 14 原文 | 主读裸 `3/2/1`，无符号组 |
| F14-6 | **数字当唯一完成** | 14 原文 | 卸符号只留数字 |
| **S14-1** | **只剩数字无隐喻** | 语义补：F14-1/6 | 有符号也仍读成 HP / 生命值 / 血条，全无燃·弱·将灭 |
| F14-2 | **三态不可辨** | 14 原文 | 满/残/濒分不清 |
| **S14-2** | **看不出还剩几命** | 语义补：F14-2/5 | 4 秒内指认不出某座 3/2/1/0；簇数与命对不上 |
| F14-4 | **自己与对手层级混淆** | 14 原文 | 四座同大、分不清自己 |
| **S14-3** | **自己与对手层级颠倒** | 语义补：F14-4 | 自己弱、对手强喊命；或自己掉命无句、对手全屏喊 |
| F14-3 | **挡手牌或荷官** | 14 原文 | 压手牌 / 荷官 / 池 |
| F14-7 | **改了命数规则** | 14 原文 | 默认命 ≠3、回血、第 4 格当规则 |
| F14-8 | **绑了贴底硬修** | 14 原文 | 为生命改 `padB` / 环-手 / 重开 flush |

F14-5（小档不可读）仍认 14，本文不另写像素。

---

## 6. 不做

不改 `lives_default`；不放开质疑；不重开贴底硬修；不改 14 正文 / `lb_*`；不交 art / ets。

---

## 7. 会签与版本

配合 #100：14 会签线框，本文会签措辞。鸿蒙等两份齐后再开客户端。

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-09-11 | 首稿烛火 + L1～L3 |
| v0.1.1 | 2026-09-11 | 对齐 #100：引用 14 `lb_*`；失败短句对 F14-*；杯/筹改一句备选；删线框复述 |

### 审核记录

| 日期 | 意见 | 提议修改 | 提出人 | 状态 |
|------|------|----------|--------|------|
| 2026-09-11 | 配合 #100，只交语义，不重复线框 | 升 v0.1.1 | 策划 | 待与 #100 会签 |

---

*相关：[14](./14-局内生命显示规格.md) · [GDD §2.2](../02-游戏设计/GDD.md) · [手牌触摸出牌-交互方案](../02-游戏设计/手牌触摸出牌-交互方案.md)*
