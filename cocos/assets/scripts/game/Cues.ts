/**
 * 表现数字锁值 · 真源仍是 docs（对局-状态机 R4 / 布局 11）。
 * 与 PlayFlyFx.ets / TableLayout.ets 对齐。本文件只常量，不写场景。
 * 禁止改 1000 / 3000 / 24。
 */
export const DRAW_TO_FLIP_MS = 1000;
export const REVEAL_HOLD_MS = 3000;
export const FLY_MS = 320;
export const HAND_RING_GAP_PCT = 24;
